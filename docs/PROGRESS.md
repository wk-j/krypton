# Implementation Progress

> Last updated: 2026-07-05

## Overview

| Milestone | Status | Progress |
|-----------|--------|----------|
| M0 — Scaffold | Complete | 5/5 |
| M1 — Single Session | Complete | 6/6 |
| M2 — xterm.js Integration | Complete | 5/5 |
| M3 — Compositor & Windows | In Progress | 9/11 |
| M4 — Keyboard System & Workspaces | In Progress | 12/14 |
| M5 — Tabs & Panes | Complete | 6/6 |
| M6 — Config, Theming & Custom Themes | In Progress | 7/9 |
| M7 — Sound Effects | In Progress | 10/11 |
| M8 — Polish | In Progress | 18/19 |
| M9 — Release | Not Started | 0/4 |

---

## Recent Landings

- **Claude Fable weekly usage meter (spec 187)** — the OAuth usage payload now
  carries model-scoped weekly buckets (Fable today) only in its new `limits`
  array; the legacy top-level fields never see them. `usage.rs` parses
  `kind: "weekly_scoped"` entries into `ClaudeUsage.weeklyScoped`
  (`{ name, utilization, resetsAt }`, generic over the model display name, so
  future scoped buckets surface with zero code change; scoped entries supersede
  matching legacy `seven_day_opus/sonnet` fields; `serde(default)` keeps old
  disk caches loading). The usage view renders one `week · fable` gauge per
  entry and `summarizeUsage` adds matching chip quotas, so window-chrome credit
  chips (spec 153) inherit the meter for free. Chose direct `limits`-array
  parsing over orca's approach (guessed OAuth field names + hidden-PTY `/usage`
  panel scraping) — the JSON is already authoritative. See
  `docs/187-claude-fable-weekly-usage.md`.

- **Built-in MCP tool reference page (spec 186)** — a fifth read-only loopback
  surface: `GET /tools` serves a static shell (`src/acp/artifact-tools.html`,
  Binance dark) that fetches `GET /tools.json`. No manifest push, unlike spec
  185: the tool descriptors already live in Rust as the exact `tools/list`
  payload (`bus_tool_descriptors()`), so `handle_tools_json` serializes them
  directly and injects a page-only `category` per entry (`tool_category()` —
  never added to the MCP response, which strict clients parse by shape). The
  page groups the 12 tools into memory / peering / artifacts / attention /
  review / issues sections; each card renders a 2-line excerpt, a params table
  built from `inputSchema` (required stars, enum/default/bounds chips,
  array-item shapes), and the **verbatim agent-facing description** in an
  expander — the behavioral contract (peer_send lifecycle, attention
  calibration) previously readable only in source. Works with zero harnesses
  registered (compile-time data). New `#tools` palette entry opens it (mirrors
  `#commands`). Drift guards: a Rust test pins every descriptor to a
  non-`other` category, and a page smoke test renders descriptor-shaped
  fixtures through the real inline script. See
  `docs/186-mcp-tool-reference-page.md`.

- **Built-in `#` command reference page (spec 185)** — a fourth read-only loopback
  surface: `GET /commands` serves a static shell (`src/acp/artifact-commands.html`,
  Binance dark) that fetches `GET /commands.json` — a **command manifest** the
  frontend builds from the *same* `HASH_COMMANDS` roster and the *same* exported
  prompt builders the dispatch calls (`buildCommandManifest()` in
  `hash-commands.ts`), pushed once per harness register via
  `acp_store_command_manifest` into a single global slot (compile-time data, last
  write wins). The page groups all 23 manifest entries (21 palette + 2
  dispatch-only; `#console` rides the `#orchestrator` entry as an alias) into
  session / surface / orchestration sections with badges (`workflow`, `agent`,
  `not in palette`,
  `alias #console`), workflow-anatomy blocks with lane cost, and — the reason the
  manifest exists — an expandable **system prompt** template on the ten
  prompt-backed commands (`#goal set`, `#handoff`, `#resume`, `#wiki`, `#recall`,
  `#directive`, `#review`, `#polly`, `#debby`, `#fix-issue`), placeholder args as
  `<token>`. New `#commands` palette entry opens it (mirrors `#docs`). The prompt
  builders moved from `acp-harness-view.ts` to a new leaf module
  `src/acp/harness-prompts.ts` (re-exported for existing import sites) so the
  manifest can render them without a circular import; `buildFixPrompt` became the
  exported pure `issueFixPrompt`. Manifest coverage tests in
  `hash-commands.test.ts` are the drift guard. `tsc` + 411 vitest + 143 cargo
  tests green. See `docs/185-hash-command-reference-page.md`.

- **Orchestrator console — global permission queue (spec 184)** — pending worker
  permissions now render as **global fleet state** in the console and are answered
  **without switching lane**. A queue band sits above the body whenever any live
  lane is awaiting permission — one row per awaiting lane (its head request), grid
  order, the **focused** row ringed as the `a`/`r` target. `a`/`A` accept (+all),
  `r`/`R` reject (+all) answer the **focused queue item** (`answerConsolePermission`,
  reusing `resolvePermission`) and **do not** `activateLane`, so confirming never
  moves the operator's vantage; `Tab`/`Shift+Tab` step the focus. This supersedes
  spec 181's *selected-card* answer surface (the `⚠ perm` card tag stays); the
  high-risk follow-up is preserved per row (full untruncated command +
  `⚠ high-risk`). While any permission is pending, `r` is reject (shadowing restart),
  mirroring the lane view's precedence but fleet-wide; `j/k` still select cards.
  The summary gains `· N perm`. New state is just a focus cursor
  (`orchestratorPermFocusId`); no Rust command / MCP tool / lane state. Frontend-only
  (`acp-harness-view.ts` + `orchestrator-console.css`); `tsc` + 275 acp vitest green.
  See `docs/184-orchestrator-console-global-permission-queue.md`.

- **Orchestrator console — prompt the seat (spec 182)** — A second send verb in
  the console: `i` opens a one-line input that sends a **normal user turn to the
  orchestrator seat** (the lane you're seated on), distinct from `d` dispatch (a
  `peer_send` to *another* lane). Routed through a new shared `submitLanePrompt`
  helper extracted from `submitActiveLane`, so `#`-commands, `!`-shell, mention
  fan-out, and the spec-136 busy-queue behave exactly as in the lane composer; the
  composer keeps its draft lifecycle via a `clearComposer` callback. Seat-scoped
  (ignores the `j/k` selection); blocked only when there is no live seat
  (`seatPromptDisabledReason`, unit-tested). Frontend-only (`acp-harness-view.ts`,
  reuses the dispatch CSS); `tsc` + vitest green. See
  `docs/182-orchestrator-console-seat-prompt.md`.

- **Attention acknowledge sends feedback (spec 183)** — Acknowledging a flagged
  judgement item (`a` in the triage overlay) is **no longer silent**: it now
  notifies the flagging lane that its `chosen` path is approved via
  `InterLaneCoordinator.deliverAcknowledge(laneId)` (same `injectHarnessEnvelope`
  delivery as redirect, drains on the lane's next idle), then clears the item as
  before. The envelope is **no-op-friendly** — a lane whose flagged work is done is
  told no reply/new work is needed, so a confirmation never forces a vacuous turn.
  Reverses the v1 "acknowledge = pure bookkeeping, no lane effect" decision: a human
  sign-off should reach the actor (like a PR "Approve"), not just clear the queue.
  Frontend-only (`inter-lane.ts` + `acp-harness-view.ts` + `attention-overlay.ts`
  hint); `deliverAcknowledge` unit-tested; `tsc` + vitest green. See
  `docs/183-attention-acknowledge-feedback.md`.

- **Orchestrator console (spec 180)** — A privileged, **behavior-neutral** lane
  role (the **Orchestrator**, ≤1 per harness) and a full-surface in-app
  **Orchestrator console** opened from it with `#orchestrator` (alias `#console`).
  The console is the *acting* in-app counterpart to the read-only lane monitor
  dashboard (168): a **region shell** (lane grid · orchestration feed · dispatch)
  plus a **reserved region** future panels (task list, delegation graph) slot into,
  à la the spec-111 rail slots. Designating an orchestrator only sets a prompt-free
  seat field (`orchestratorLaneId`, **not** `pollyBuiltinRole`, which injects a
  prompt), badges the lane (lane-head `◆ orch` chip + console badge), and reserves
  it as the home for future orchestrator-only tools — it injects no prompt;
  autonomous fan-out stays opt-in via the existing `#polly`. The console
  **dispatches** work (`deliverMentionFanOut` single-target = a `peer_send`, never a
  Goal-set, so workers keep context; purpose-tagged
  `[implement|review|explore|search]`) and **overrides** lanes by reusing existing
  primitives (`cancelLane`/`closeLane`/`restartLane` — no pause/resume, which does
  not fit ACP). Keys: `j/k` select · `Enter` jump · `d` dispatch (`Tab` cycles
  purpose) · `c/x/r` interrupt/kill/restart · `o` transfer seat · `Esc` close.
  Entry: `#orchestrator`/`#console` (primary, discoverable) **and** leader key
  `` Leader ` `` (backtick — `Leader Shift+O` collides, `o`/`O` are reserved global
  keys, so the free punctuation key adjacent to the harness cluster substitutes per
  the spec-124/127/128 precedent). Division: **AI owns
  coordination, human owns judgement + the kill switch.** Governed by
  `docs/adr/0011-orchestrator-privileged-lane-and-acting-console.md` (first
  asymmetric lane role + first mutating cross-lane surface, deliberately bounded);
  terms in `CONTEXT.md` (**Orchestrator**, **Orchestrator console**, **Dispatch**).
  Frontend-only (`acp-harness-view.ts` + `src/styles/orchestrator-console.css` +
  `hash-commands.ts`); dispatch helpers unit-tested (`tsc` + `vite build` + 140 acp
  vitest green). Deferred: orchestrator-only MCP tools, AI-driven command of other
  lanes. See `docs/180-orchestrator-console.md`.

- **Orchestrator console — permission action (spec 181)** — a worker lane paused
  on `needs_permission` can now be **answered from the console** without jumping
  to it, reusing the existing `resolvePermission(lane, action, auto)` primitive
  (no new permission path / Rust command / MCP tool / lane state). Every
  `needs_permission` lane card gets a `⚠ perm` tag; the **selected** card shows the
  compact tool label + hint and `a`/`A` accept (+accept-all), `r`/`R` reject
  (+reject-all) answer the head request — permission keys take **precedence** on the
  selected card exactly as in the lane view, so `r` is reject (shadowing restart)
  while pending. **High-risk (revised):** a **high-risk** command (spec 140/143
  classifier) is now **accepted inline from the console too** — no lane jump required.
  The first cut blocked it; per the operator's call the whole confirm lives in the
  console, and the safety is preserved differently: the selected-card strip surfaces
  the **full, untruncated command** (`extractCommandLineRaw`, the same source the
  classifier reads, so a destructive tail past the 48-char label is never hidden)
  under a `⚠ high-risk` marker, so the dangerous accept is informed in place rather
  than deferred to the lane. `Esc` stays close-only (never rejects). Decision
  helper `consolePermissionAction({ pending, action })` is unit-tested. Multi-reviewer review (Cursor-1 +
  Codex-1, 0 blockers) → follow-up fixes folded in: the console is refreshed
  directly at every permission-queue mutation (`refreshOrchestratorConsole()` — the
  `LaneBus` only fires on status transitions, so a queued answer / stacked request /
  rollback that stays `needs_permission` would otherwise leave the strip stale), and
  the flag-arming is an extracted, unit-tested pure helper (`armConsolePermissionFlags`).
  Frontend-only (`acp-harness-view.ts` + `orchestrator-console.css`); `tsc` + vitest
  green. See `docs/181-orchestrator-console-permission-action.md`.

- **Artifact gallery newest-first ordering (spec 170)** — `/artifacts` now lists
  each harness's artifacts **latest-creation-first** (newest at the top) instead of
  `laneLabel`-then-`id`. `list_all_artifacts_for_gallery()` sorts on the parsed
  `art-<seq>-<hex>` seq descending (ids aren't zero-padded, so the raw string would
  mis-order `art-10` vs `art-2`); test renamed to
  `gallery_orders_artifacts_latest_creation_first`. See
  `docs/170-artifact-gallery-endpoint.md`.

- **`github.dispatch-issue` multi-harness routing (spec 178)** — A dispatch carries
  its target as `targetLane` (a globally-unique lane displayName) — never
  `lane`/`harnessId` — so `control-bridge.route()` resolves the owning harness from
  the displayName and routes there; `__new__`/absent falls back to the sole-harness
  rule and errors `ambiguous_harness` when two or more are open. Fixes the dispatch
  analogue of #8 (`github.dispatch-issue requires harnessId`). `route()` is now
  exported for `src/acp/control-bridge.test.ts`. See `docs/178-github-issue-fixing.md`.

- **Review quality matrix structured findings detail (spec 146 / ADR-0004 update)** —
  `review_outcome` remains the count-first, session-only observation row for
  `#review`, but now accepts optional embedded `findings[]` detail capped at 500
  items: `{ file, line?, severity: blocking|non-blocking|suggestion, note }`.
  The frontend validates the IPC payload strictly/all-or-nothing, stores findings
  only in the in-memory `ReviewQualityStore`, and renders them in the review
  matrix as keyboard-expandable row detail (`j`/`k` select, `Enter`/`Space`
  expand, `h`/`l` switch lanes). ADR-0004 is superseded in part: findings are
  evidence, not a score, verdict, alarm, rank, diff-size metric, transcript
  anchor, or persistent review session. Prompt guidance for `#review` and
  `#polly` now asks reviewers for file:line findings and tells the convening lane
  to pass structured findings to `review_outcome`. See
  `docs/146-review-quality-matrix.md` and
  `docs/adr/0004-review-matrix-observation-not-score.md`.

- **`#polly` Omnigent supervisor-contract refresh (spec 164 rev 8)** — The
  `#polly` role and request prompts now mirror the latest Omnigent Polly
  supervisor contract where it fits Krypton's live-lane architecture: the
  orchestrator is explicitly not the coder/investigator/reviewer, coding work
  and real investigation are delegated, each worker `peer_send` must carry a
  task title, purpose (`implement`/`review`/`explore`/`search`), scope,
  acceptance, gates, and report shape, cross-review stays different-worker
  diff+contract only, blocking findings loop back to the same implementer
  thread, and synthesis must read worker reports plus deterministic gates rather
  than infer success from `git status`. This does not port Omnigent's separate
  worktrees/PR registry mechanics; Krypton `#polly` remains shared-worktree live
  ACP lanes. See `docs/164-polly-orchestration.md`.

- **YouTube transcript extraction (spec 179)** — The popup's on-demand
  extractor now runs Defuddle's **async** parse (`parseAsync()` instead of the
  sync `parse()`), unlocking Defuddle 0.19.1's built-in YouTube extractor, which
  fetches the transcript via the InnerTube API (DOM-panel fallback). No custom
  transcript-fetching code — same engine Obsidian Web Clipper uses. The same
  async switch also reaches Defuddle's X/Twitter `x-oembed` extractor; host
  permissions added for `https://www.youtube.com/*` and
  `https://publish.twitter.com/*` (the X oEmbed endpoint). Runtime verification
  on real pages still pending (cannot launch the Chrome extension in CI). See
  `docs/179-youtube-transcript-extraction.md`.

- **GitHub issue fixing (spec 178)** — "Fix this issue" is a single, surface-agnostic
  operation: it can be started from **anywhere** — Krypton's command palette ("Fix
  GitHub Issue…") or the `#fix-issue <url | owner/repo#123>` verb, the extension
  popup on any page, or a button injected onto the GitHub issue page — and every
  surface can read its status. All entries converge on one frontend path,
  `dispatchIssue()` (also reachable via the `github.dispatch-issue` control op),
  which records an **issue↔lane binding** (harness-level map keyed by `owner/repo#123`),
  sets the lane goal/badge, and prompts a fresh dedicated lane (default) to fix it.
  Metadata for non-DOM surfaces comes from the local `gh issue view --json` with a
  URL-only fallback. The lane self-reports progress via a new `issue_report
  { phase, summary, pr_url }` MCP tool (bus round-trip mirroring `attention_flag`).
  Status is **snapshot-first + stream**: `github.issue-status` returns the persisted
  binding merged with live lane status (refresh-safe), and an `issue_status` SSE
  event pushes live deltas; the extension injects a live status card (unbound /
  working / needs-permission / done / offline) into the issue sidebar. Bindings are
  disk-backed (`acp_save/load_issue_bindings`, atomic write next to the memory file,
  rehydrate-on-register) so they survive a Krypton restart — the lane is then gone,
  so the snapshot reports `stopped` and the card offers re-dispatch. New control ops:
  `github.dispatch-issue / issue-status / list-issues / unlink-issue` (the
  issueKey-addressed reads fan out across harnesses in `control-bridge`). Read-only
  overlay only — no write-back to GitHub in v1. See `docs/178-github-issue-fixing.md`.
- **Streaming-markdown table seal re-render (spec 117 follow-up)** — Fixed GFM
  tables rendering broken in assistant messages: streaming-markdown's single-pass
  table parser desyncs on a mid-table stream-chunk boundary and freezes literal
  `| … |` text at seal. Seal now does a **guarded post-seal swap to `marked`** —
  `hasMarkdownTable()` detects a delimiter row and only then re-renders the message
  with `md.parse` (full GFM); table-free messages keep the cheaper smd output, so
  the common path and the per-token streaming hot path are unaffected (one-shot at
  turn-end). Branch A preserves the lane-mail provenance node. `tsc` green; 125
  acp-harness-view tests pass (3 new `hasMarkdownTable` cases). See
  `docs/117-streaming-markdown.md`.
- **Extension Obsidian-grade content extraction (spec 177)** — When the popup is
  opened with **no selection**, the extension now extracts the page's main content
  as Markdown client-side instead of sending URL-only, so pages a lane can't fetch
  server-side (Reddit, YouTube, login-walled, JS-rendered SPAs) still work. Uses
  **Defuddle** (`defuddle/full`, the library behind Obsidian Web Clipper) bundled by
  esbuild into `extension/dist/content.bundle.js` and injected on demand via
  `chrome.scripting.executeScript` (keeps the no-persistent-content-script model of
  spec 176). A selection always wins; `{page}`/`{author}` template placeholders added;
  the full extracted page is sent (no length cap — Open Question #2). New `extension/package.json`
  + `make extension` build step (run from `make install`). See
  `docs/177-harness-extension-content-extraction.md`.
- **Harness browser extension + native-messaging bridge (spec 176)** — A
  Chrome/Chromium MV3 extension (`extension/`) that sends the current page
  selection into a running harness lane as a chosen *action* (predefined prompt:
  Explain / Summarize / Translate / Find issues / Custom…, editable in the
  options page). It is a thin client of the control API (doc 175): the popup
  reads `lane.list` to pick a lane and `POST`s `lane.send`. **Zero-config token
  discovery** via a new Native Messaging host — `krypton-bridge`
  (`src-tauri/src/bin/krypton-bridge.rs`) reads the `0600` control descriptor as
  the user and returns `{url, token, pid}` over the stdio length-prefixed
  protocol; only the extension whose pinned ID matches the manifest's
  `allowed_origins` can launch it. Krypton writes the host manifest into the
  browser's `NativeMessagingHosts` dir on launch (`native_host.rs`, gated by
  `[acp_controller].install_native_host` + `native_host_browsers`). The control
  API now binds a **fixed port 8766** (`[acp_controller].port`, ephemeral
  fallback) so the extension needs no port discovery. Selection captured via
  `chrome.scripting.executeScript` (no persistent content script). `make install`
  ships `krypton-bridge` alongside `kryptonctl`. Backend build + clippy + fmt +
  3 native_host tests green; extension JSON/JS validated. See
  `docs/176-harness-browser-extension.md`.
- **Harness web control API + SSE stream (spec 175)** — Extends the existing
  authenticated control server (`control.rs`, doc 154) so a separate same-machine
  web app can remote-control a running harness with full feature coverage and a
  live feed (Option A; frontend stays the authority per ADR-0007). Adds **(1)** a
  `GET /control/v1/events` Server-Sent Events stream — the frontend pushes the
  harness events it already processes (`src/acp/acp-harness-view.ts` event sink +
  status transitions) through a new `acp_control_publish` command into a
  per-server `broadcast` channel that Rust fans out to subscribers (auth via
  bearer header or `?token=` for `EventSource`; `ready`/`gap`/heartbeat frames;
  text-delta coalescing to ~30 ms in `src/acp/control-publish.ts`); **(2)** new
  read operations `attention.list/resolve`, `artifact.list`, `lane.status`,
  `lane.commands`, `lane.metrics`; **(3)** opt-in exact-origin CORS
  (`[acp_controller].cors_origins`, empty = proxy-only secure default — never
  `*`). This retires the SSE capability doc 154 had marked deferred. 8 Rust
  control tests + `npm run check` + clippy green. See
  `docs/175-harness-web-control-api.md`.
- **Docs browser artifact export (spec 174)** — The `/doc` reader can now ask
  the active harness lane to generate a normal HTML artifact from the markdown
  file the user is reading. A fixed `artifact` pill and the `a` shortcut POST
  `{ batchId, title }` to `POST /doc-artifact?harness=&path=`; Rust validates
  the same repo-relative `.md` path as docs feedback, caps the title with the
  artifact title limit, emits `acp-docs-artifact-requested`, and waits for the
  existing `acp_bus_reply` round-trip. The frontend routes to the active live
  lane, de-dupes by `batchId`, appends a system row, and drains a
  `DocArtifactRequestQueue` on the next `idle`/`awaiting_peer` transition. The
  injected task tells the lane to read the source markdown, call `artifact_new`,
  edit the scaffold, and call `artifact_register`, so registry cards, write
  grants, feedback tokens, and redaction all stay on the existing artifact path.
  ADR-0010 is amended for this second tokenless write-side docs action. See
  `docs/174-docs-browser-artifact-export.md`.
- **Lane monitor attention flag details** — The browser dashboard (`/dashboard`) now shows open `attention_flag` items directly under the attention gauge instead of only the aggregate flag count. `TelemetrySnapshot.attention` gained a schema-v3 `items[]` list with lane name, reversibility, question, chosen path, uncertainty, and trade-offs; the fixed dashboard page renders those rows in the existing Binance-dark data style and keeps the per-lane `attn` chips/count gauge unchanged. The schema constants were bumped in lockstep (`TELEMETRY_SCHEMA_VERSION` and dashboard `SCHEMA_VERSION` 2→3), and the telemetry test suite covers the new payload shape. See `docs/168-harness-lane-monitor.md`.
- **Artifact gallery disk rehydration (spec 173)** — Reverses spec 170's "registry = active set, NOT disk" decision per explicit user direction (no retention, every artifact must stay feedback-capable, one uniform pattern, ignore the on-disk `harnessId`). `init_harness_artifacts` now rebuilds the in-memory registry from the **entire** on-disk tree under the project's `.krypton/artifacts/` — every `*/<lane>/<id>.html`, regardless of which `harnessId` subdir it physically lives under — and **re-homes** each entry under the currently-live harness so its feedback token routes through the existing path exactly like a same-session artifact. Title + feedback token are parsed back out of the file (`parse_artifact_title` / `parse_feedback_token`; the token is never re-minted — the served page POSTs with the baked one), size/hash recomputed, `next_artifact_seq` bumped past the max rehydrated seq. A new `acp_list_harness_artifacts` command lets the frontend **pull** the rehydrated entries in `initializeHarnessMemory` (`refreshArtifacts()`, mirror of `refreshMemory`) and replay them through the existing `handleArtifactEvent` — necessary because rehydration runs in `register_harness` before the `acp-harness-artifact` listener attaches, so init-time events would be lost. This populates the frontend `this.artifacts` mirror (the gate the feedback handler checks) and raises a card under any matching live lane. Inherent (not a separate code path): feedback only *lands* when the artifact's lane label matches a currently-live lane — same as a same-session artifact whose lane stopped. 41 Rust hook_server tests (2 new: cross-restart rehydration + parse-helpers) + 251 acp vitest + clippy + tsc green. See `docs/173-gallery-disk-rehydration.md`.
- **Docs browser inline feedback (spec 172)** — The read-only docs browser (spec 171) gained the *feedback* direction, generalizing artifact inline feedback (spec 149) to repo markdown. The `/doc` reader self-injects a point-and-comment overlay (client-side, gated on `location.pathname === "/doc"`; the `/docs` index never arms it): `c` arms an element/text picker, click anchors a passage, `s` sends, `j/k` cycle, `sessionStorage`-persisted unsent pins keyed by `harness+path`. Three adaptations follow from docs being repo files, not lane-owned artifacts: **(1)** a doc has no owning lane, so feedback routes to the harness's **active** lane (resolved by the frontend at delivery, redirectable by switching the active lane in-app; no live lane → `409`); **(2)** **tokenless** — `POST /doc-feedback?harness=&path=` is keyed by the same addressing the read uses (Q1=A, consistent with ADR-0010's posture + the already-reachable loopback turn-injection), running `validate_doc_path` so only a real `.md` under `<cwd>` is a valid target; **(3)** the anchor is the **quoted text + enclosing heading trail**, not a `cssSelector` — the page is comrak output, so the lane locates the passage in the *source* `.md` by the quote. Reuses spec 149 end to end: a `BaseFeedbackQueue<E>` was extracted so `ArtifactFeedbackQueue` and the new `DocFeedbackQueue` share the drain-on-`lane:status`-idle core (only `composePrompt` + recipient differ); the synchronous bus round-trip (`register_bus_reply` → emit `acp-docs-feedback-received` → await `acp_bus_reply`); and the `<doc-comment>` untrusted-data prompt framing (server caps count/body/quote → `413`). A sibling `GET /doc-state` returns the file sha256 for the overlay's live-reload poll (**amends spec 171 decision #9 — no polling — for `/doc` only**). ADR-0010 amended (the surface is no longer strictly read-only; the cheap per-session-nonce hardening was declined as Q1=B and noted for future). `npm run check`, `cargo clippy`, 123 Rust + 251 acp vitest tests green. See `docs/172-docs-browser-inline-feedback.md`.
- **Artifact gallery web endpoint + append-only artifact files (spec 170)** — A read-only, loopback-only **artifact gallery** sibling of the lane-monitor dashboard (spec 168): `GET /gallery` serves a fixed standalone HTML page (`src/acp/artifact-gallery.html`, embedded via `include_str!`), and `GET /artifacts` returns token-free JSON `{ harnesses: [{ harnessId, artifacts: [{ id, laneLabel, title, state, size, hash, tail, token }] }] }` (both pending + live, originally sorted by harnessId → laneLabel → id; the within-harness order was later changed to latest-creation-first — see the newest-first landing above). The page polls `/artifacts` ~1 s (JSON-signature stale check, no DOM thrash), renders a harness-grouped card grid in the dashboard's exact Binance-dark shell, and each live card links same-origin to `/artifact/<encodeURIComponent(token)>`. Open wiring mirrors the dashboard: `compositor.openGallery()`, command palette `gallery.open`, and the `#gallery` harness composer command (no keybinding — the dashboard's `Leader Shift+L` slot is taken). Backed by `HookServer::list_all_artifacts_for_gallery()`. The gallery reads **only the in-memory registry** — a disposed harness delists automatically (active-set view). Resolved fork per human direction: **artifact files are never deleted** — all four file-deletion paths removed (`dispose_harness_artifacts` no longer `remove_dir_all`s the harness dir; `sweep_stale_artifacts` function + call + test fully deleted and `init_harness_artifacts` lost its `live_harness_ids` param; both pending-cancel paths dropped their `remove_file`), keeping only the in-memory registry/token/telemetry cleanup. Model: disk = append-only history, registry = active set. **Documented trade-offs:** `/artifacts` returns the feedback token (so artifacts are enumerable over loopback — the token is also a turn-injection cap via `POST /artifact/feedback/<token>`; shipped as Option A, consistent with the `/telemetry` loopback posture); on-disk growth is now unbounded with no in-app GC (manual `rm -rf .krypton/artifacts/` only); persisted files are not browsable after restart (no disk rehydration — by design). Cross-reviewed across 3 slices + the deletion-stop slice (4 reviewers total, 0 blockers). 31 Rust tests (7 gallery/preservation tests) + tsc clean. See `docs/170-artifact-gallery-endpoint.md`.

- **`#debby` harness wiring (spec 167)** — `#debby <question>` is now registered in the ACP harness hash-command palette and dispatch path. The active lane becomes the Debby orchestrator; `ensureDebbyHeads` finds or spawns separate Claude and Codex head lanes (always excluding the orchestrator lane id, so a Claude orchestrator still gets a second Claude head), stamps Debby role prompts via `renderPromptMemoryPacket`, re-activates the orchestrator, and injects `debbyRequestPrompt` for peer_send fan-out. Debby heads are plain responders: no Polly-style `permissionMode: bypass`, no saved permission-mode state. `npm run check` plus the Polly/Debby targeted Vitest command are green.
- **`#polly` always emits a live plan (spec 166)** — A `#polly` run now surfaces a glanceable, live todo/progress list the human can watch, reusing the harness's existing **Plan panel** (`renderPlanPanel`, which renders any ACP `plan` `session/update` — progress bar, `step N of M`, per-entry `pending`/`in_progress`/`completed`). Prompt-only change in `src/acp/polly.ts`: the orchestrator role prompt + `pollyRequestPrompt` steps 1 (emit one plan entry per task slice) and 5 (flip statuses as slices land) instruct the orchestrator to keep a live plan. No new UI/store/MCP tool. Best-effort (ACP plans are agent-emitted — not harness-enforced), but all Polly backends (cursor/claude/codex) support the plan channel (`docs/113:131`). `npm run check` + acp tests green. See `docs/166-polly-live-plan.md`.
- **Scope memory to handoff only (spec 165)** — The harness memory tools (`memory_set`/`memory_get`/`memory_list`) are no longer framed as an ambient shared scratchpad. `renderPromptMemoryPacket` no longer advertises memory on any turn (the peering paragraph stays); the Rust `bus_tool_descriptors()` descriptions were reworded from "a living README other agents read" / "discover what other agents are doing" to a **handoff store** for `#handoff`/`#resume`, with a stale-snapshot caveat on `memory_get` (verify against the live repo before acting) and the reference-by-path discipline on `memory_set`. Rationale: in steady state a lane already holds its own working state, and reading a frozen snapshot risks acting on stale truth (cache-coherence hazard) — the durable, deliberate use is cross-session handoff. Cross-lane *read* is retained (read any lane's handoff; write only your own); no tool logic, persistence, drawer, or command changed. After review (Cursor-4: 0 blockers, 6 warnings), the user confirmed **no exceptions** — so `#polly` (spec 164) was realigned too: the orchestrator role prompt dropped "edit your lane memory" and step 5 no longer maintains a `## Polly tasks` scratchpad in `memory_set` (it tracks task/worker status in its own working context). Stale doc claims fixed (specs 98/139 banners + `memory_list` "full roster" correction). `npm run check` + `cargo build` green. See `docs/165-memory-handoff-only.md`.
- **`#polly` orchestration + hash-command palette (spec 164)** — `#polly <task>` runs from any lane: the active lane is the Polly orchestrator; `ensurePollyWorkers` auto-spawns up to two workers from the fixed `cursor/claude/codex` pool, excluding the orchestrator backend when it is in the pool (outside-pool orchestrators use Cursor + Claude), so each run is capped at 2 workers + orchestrator = 3 lanes. It applies built-in `pollyBuiltinRole` overlays in `buildPromptBlocks`, gives implementer lanes temporary `permissionMode: bypass` for unattended work, restores their prior mode when the role clears, re-activates the orchestrator after spawn, and injects `pollyRequestPrompt` (agent-orchestrated `peer_send` fan-out — sibling of `#review`). Also landed the **`#` command autocomplete palette** (`hash-commands.ts` registry + composer popup mirroring `/` and `@`). `npm run check` green; see `docs/164-polly-orchestration.md`.
- **Directive picker — concise single-line rows (follow-up to spec 163)** — Rebuilt the directive picker row from a 3-line block (title / `id · task` meta / description) into one dense line — `icon · title · badges · dimmed truncated description` — so many directives stay scannable without scrolling; the `id · task` meta moved to the preview head, the legacy triage tag collapsed to a glyph (`◆`, full label in tooltip), and the list grew to `52vh`. Dropped the `directive-main` wrapper (flatter markup) and deleted the dead `directive-logo*` (~27 CSS lines, unused since spec 163) and `directive-meta` rules. Keybindings and assignment behaviour unchanged. `npm run check` green, 113 acp tests pass.
- **Generic, backend-agnostic directives + active-lane fast switch (spec 163)** — Harness directives are no longer locked to a backend. The `backend` field was **removed** from `HarnessDirective` (TS + Rust struct), along with `BUILTIN_BACKEND_IDS`, its validate branch, the two backend-rejection tests, the `harnessDirectives()` Gemini filter, and the directive-row backend logo/scope; `directiveCompatible(directive, lane)` became `directiveAssignable(directive)` = enabled-only, so any enabled directive assigns to any lane (picker, `lane.directive` control op, `#directive`). Stale `backend = "..."` lines in an existing `acp-harness.toml` are ignored on load (serde, no migration/version bump). The **directive picker** now distinguishes two actions: **Enter** switches the focused lane's directive in place (busy lanes defer via `assignDirectiveToLane`'s `pendingDirectiveChange`), and **Shift+Enter** spawns a new lane — `addLaneFromDirective` was removed in favour of setting `pendingSpawnDirectiveId` and opening the existing lane backend picker, whose Enter calls `addLane(backendId, directiveId)`; the backend-picker header shows `directive: <title>` while a spawn is pending, and the pending id is cleared on every picker close/failure. `directivePrompt` (the `#directive` author schema) dropped its `backend` field guidance. `npm run check`, `cargo clippy`/`fmt`, 9 Rust config tests, and 113 acp frontend tests all green. See `docs/163-generic-directives.md`.
- **Review priority roll-up — footer indicator + summon overlay (spec 162)** — `mark_review_priority` (spec 160) reports, previously visible only inside the Diff Window, now also surface at the harness level, mirroring the review quality matrix (spec 146). The inline `reviewPriorityReports` Map was promoted to a `ReviewPriorityStore` (`src/acp/review-priority-store.ts`) that owns the per-lane reports, exposes `highCount()`/`lanesWithReports()`/`reportFor()`/`allRanges()`, and emits a `review:priority` LaneBusEvent on every mutation; the `diff.review-priority` control op now reads `allRanges()` (diff behaviour unchanged). The harness publishes the total `high`-range count over a new `review:priority` ViewBus signal → a **neutral `N priority` footer indicator** (hidden at zero, never coloured — advisory, ADR-0009) beside the `N reviews` chip. A summon-on-demand **read-only overlay** (`Leader /`, j/k to switch lane, Esc/q close; mutually exclusive with the triage and review-matrix overlays) rolls up each lane's reported ranges grouped read-first/routine — acting on a range still happens in the Diff Window's `p` panel. New `ReviewPriorityStore` (6 tests) + overlay renderer; `npm run check` green, 204 acp tests pass. See `docs/162-review-priority-roll-up.md`.
- **Directive management without dedicated MCP tools (spec 161)** — Removed the four always-on directive MCP tools (`directive_list`, `directive_preview`, `directive_apply`, `directive_remove`) from the harness bus, reclaiming **~1,224 tokens/turn** of tool-schema overhead advertised to every lane every turn (a directive is authored rarely, so the schemas were dead weight). Authoring is now the one-shot **`#directive <intent>`** composer command (the `#wiki` pattern): it injects the config path + TOML schema + safety rules and the focused lane edits `~/.config/krypton/acp-harness.toml` with its **own** read/edit/write tools — tokens cost only when invoked. No file watcher (the directive picker `Cmd+P → .` already re-reads disk on every open). `acp_harness_config::load()` is now **per-entry lenient** — one malformed `[[directives]]` entry is skipped with a warning instead of breaking the whole file. The dead frontend round-trip surface (`acp-directive-apply-requested` / `acp-harness-directives-changed` listeners, the approval card, `handleDirectiveApply`/`handleDirectiveAssign`/`resolveDirectiveApproval`, `unifiedPromptDiff`) was removed; stray cached tool calls return a "use `#directive`" error. Lost capability: agent-initiated cross-lane `assign` (user-driven picker assignment stays). `cargo clippy`/`fmt`/`test` (110 tests) and `npm run check` all green. See `docs/161-directive-tools-on-demand.md` and `docs/124-acp-harness-directive-management.md`.
- **Gemini removed from ACP Harness** — The multi-lane harness now filters Gemini out of its lane picker and session picker, rejects Gemini-scoped harness directives, and removes Gemini-only harness logo/tint mappings. The shared ACP backend remains registered, so the standalone Gemini ACP agent window and its `Leader Shift+E` shortcut continue to work.
- **ACP Harness Cline lane (spec 159)** — The Cline CLI is now a built-in ACP backend, the 13th lane, spawning `cline --acp` (native stdio ACP server, `npm i -g cline`, auth via `cline auth`). Touchpoints follow the MiMo/Copilot template: `builtin_backends()`, `BUILTIN_BACKEND_IDS`, `BACKEND_LABELS`, `backendLogoId` + a new `krypton-logo-cline` terminal-prompt glyph, `laneAccentForLabel` + a 13th `laneAccent` color (`#56d6c0`), and a `--krypton-backend-cline` CSS token with rail/directive tint classes. Cline delivers MCP through a **native-config overlay**, not `session/new`: a live `cline --acp` handshake (cline 3.0.24) showed `initialize` advertises **no `mcpCapabilities`**, so http/sse servers injected via `session/new.mcpServers` are dropped and the per-lane memory/peer tools never appear. Krypton instead writes a per-lane `cline_mcp_settings.json` (memory server tagged `streamableHttp` — Cline defaults a URL-only entry to SSE — plus the spec-83 project bridge) under `~/.config/krypton/runtime/cline/<harness>/<lane>/` and points `CLINE_MCP_SETTINGS_PATH` at it at spawn, leaving global `~/.cline` auth/providers shared; overlay is removed on close and GC'd on harness start (mirrors the Junie overlay; `sanitize_junie_path_component` renamed `sanitize_overlay_path_component`). Cline does expose `-m`/`--provider` CLI flags, but v1 leaves the model to `cline auth`/config, so `acp_harness.lane_models.cline.active` is chip-only unless Cline advertises model state at `session/new`. Startup hints (placed before the generic `claude /login` branch so they aren't shadowed) cover missing installs, old CLIs without `--acp`, auth (`cline auth`), token issues, and empty-stderr first-run stalls. See `docs/159-acp-cline-lane.md`.
- **ACP Harness MiMo lane** — MiMo-Code (Xiaomi's OpenCode fork, `npm install -g @mimo-ai/cli`) is now a built-in ACP backend, the 12th lane, spawning `mimo acp` (it retains OpenCode's ACP module and `acp` subcommand). Touchpoints follow the Grok/Copilot template: `builtin_backends()`, `BUILTIN_BACKEND_IDS`, `BACKEND_LABELS`, `backendLogoId` + a new `krypton-logo-mimo` "mi"-tile glyph, `laneAccentForLabel` + a 12th `laneAccent` color, and a `--krypton-backend-mimo` CSS token with rail/directive tint classes. Unlike the `opencode` lane (dedicated `session/set_config_option` path), MiMo takes the **generic** model path: its `session/new` advertises `models { currentModelId, availableModels }` (verified in the MiMo-Code source), so Krypton applies the anonymous free-for-limited-time `mimo/mimo-auto` model by default via non-fatal post-`session/new` `session/set_model`; `acp_harness.lane_models.mimo.active` overrides it. MCP delivery is the standard path: per-lane `krypton-harness-memory` + spec-83 project `.mcp.json` bridge via `session/new.mcpServers`, capability-gated. Startup hints (placed before the generic `claude /login` branch so they aren't shadowed) cover missing installs and first-launch provider setup (`mimo` once in a terminal: MiMo Auto / Xiaomi OAuth / custom provider). 5 new Rust tests; live `mimo acp` handshake unverified — the CLI is not installed on this machine. See `docs/69-acp-agent-support.md`.
- **Harness Controller CLI core v1 (spec 154)** — Added the macOS-first Rust `kryptonctl` binary and an authenticated, versioned control API on a dedicated loopback listener. Krypton publishes a rotating bearer token in permission-`0600` `~/.config/krypton/runtime/controller.json`; the CLI validates descriptor PID liveness before connecting. The frontend remains live-state authority through `src/acp/control-bridge.ts` and `HarnessDirectory`, with a bounded idempotency cache and typed Tauri round-trip. Implemented capabilities cover harness list/create; lane list/spawn/close/restart/new/send/cancel/model/directive/goal/permission-mode; `send --wait` via lane-state polling; explicit permission list/resolve; redacted transcript; memory list/get/clear; and read-only peer list. Destructive CLI operations require TTY confirmation or `--yes`. `make install` installs the app to `/Applications` and the CLI to `~/.local/bin` by default. SSE/prompt IDs, session resume, image/stdin send, and harness close remain deferred. See `docs/154-harness-controller-cli.md` and ADR-0005–0007.
- **Per-window AI credit status (spec 153)** — Supported ACP and ACP Harness views now render subscription quota telemetry in the left side of their host window's existing bottom status bar; transient notifications remain right-aligned. A multi-lane Harness publishes the deduplicated union of Claude/Codex/Copilot/Cursor lanes, while hidden tabs and unsupported providers stay absent. Normal widths show all meaningful quota windows; constrained widths retain each provider's most-used quota. `src/usage-store.ts` now owns shared ref-counted polling, so window chrome and the detailed `Leader $` usage view consume one snapshot/timer per provider instead of creating independent loops. See `docs/153-window-ai-credit-status.md`.
- **ACP Harness Copilot lane (spec 150)** — GitHub Copilot CLI is now a built-in ACP backend, the 11th lane. The lane picker lists `Copilot`, spawning `copilot --acp --stdio` (Copilot's native ACP server, public preview) with cached login-shell environment injection. Copilot is a regular permission-gated lane: the project `.mcp.json` bridge (spec 83) and the per-lane `krypton-harness-memory` MCP server both apply, capability-gated by what Copilot advertises at `initialize`. Touchpoints follow the Grok template (`builtin_backends()`, `BUILTIN_BACKEND_IDS`, `BACKEND_LABELS`, `backendLogoId` + a new `krypton-logo-copilot` goggle/visor glyph, `laneAccentForLabel`) plus the two net-new bits the 11th lane forces: an 11th `laneAccent` color (so `laneAccentForLabel('Copilot-1')` doesn't modulo-wrap onto Codex blue) and a `--krypton-backend-copilot` CSS token + `--copilot` rail/directive tint classes. Copilot-specific startup diagnostics cover missing installs (`npm install -g @github/copilot`, Node 22+), old CLIs without `--acp`, auth (`/login` device flow), token issues (`COPILOT_GITHUB_TOKEN`/`GH_TOKEN`/`GITHUB_TOKEN`), and empty-stderr first-run stalls. **Model selection is conservative**: Copilot's ACP server documents no model flag, so no spawn-time flag is passed; `acp_harness.lane_models.copilot.active` is chip-only unless Copilot advertises model state at `session/new`. **Verified live against Copilot CLI 1.0.60**: the `initialize` handshake over `copilot --acp --stdio` returns `mcpCapabilities {http, sse}` and `promptCapabilities.image` — so the per-lane HTTP memory MCP passes `filterByCapability` and Copilot advertised no model state at `initialize` (chip display-only, as expected). Whether Copilot honors `session/new.mcpServers` in practice is left unverified pending a live session + tool call (advertising the capability is not proof — Cursor regressed on exactly this, spec 113). 5 new Rust `startup_hint` tests + frontend palette/logo tests; `cargo test` (49 acp tests), `cargo clippy`, `cargo fmt`, `npm run check`, and 110 harness vitest tests all green. See `docs/150-acp-copilot-lane.md`.
- **Artifact inline feedback (spec 149)** — Closes the missing *feedback* direction of HTML artifacts (spec 133): the user can now point at an element on the rendered artifact in their browser and have that context flow back to the authoring lane, instead of re-typing the critique as composer prose. Modelled on [`crit`](https://crit.md)'s live mode but reusing Krypton primitives end to end. **Artifacts are now served over loopback HTTP, not `file://`** — `openArtifact` opens `http://127.0.0.1:<harnessMemoryPort>/artifact/<token>` served by the existing harness-memory HTTP server (new `GET /artifact/<token>` re-runs the full spec-133 validation on every serve; `no-store`/`nosniff`/`no-referrer` headers). This amends ADR-0002's open *mechanism* (the OS-browser surface + hint-keypress gate are unchanged) so the feedback page is **same-origin** with the feedback endpoint (no CORS / `Origin: null`) and gets a real origin for future SSE. `artifact_new` bakes an unguessable per-artifact **feedback token** (128-bit, `getrandom`) + base URL into the seeded scaffold (`{{feedbackToken}}`/`{{feedbackBaseUrl}}`, substituted like `{{title}}`) and records `token → {harnessId, laneLabel, artifactId}` in the registry. The scaffold's existing harness `<script>` now **self-injects** a mini element-picker overlay (hover-highlight + click-to-anchor + comment composer; `c`/`j`/`k`/`Esc`/`s` keys; `sessionStorage`-persisted unsent pins; live-reload poll of `GET /artifact/state/<token>`) — self-injecting so the lane editing `<main>` can't clobber it at runtime (best-effort: a file edit that strips the script still loses it). The overlay `POST`s a batch `{ batchId, comments }` to `POST /artifact/feedback/<token>`, which runs the **synchronous bus round-trip** (fresh request id → emit `acp-artifact-feedback-received` → await `acp_bus_reply`, same pattern as `peer_send`) so the POST reports real acceptance (`accepted`/`409 no-live-lane`/`410 revoked`/`503 retry`). `AcpHarnessView` resolves the registry `laneLabel` → live lane, de-dupes by `batchId`, and pushes onto a **dedicated feedback queue** (`src/acp/artifact-feedback.ts`, NOT the peer `LaneInbox`) that drains on the lane's next idle via `enqueueSystemPrompt` — the composed prompt wraps every comment in delimited `<artifact-comment>` blocks marked as untrusted data. Tokens are revoked forward-only on lane close / `#new` (`acp_revoke_artifact_feedback` IPC, extends `dropAllArtifactsForLane`); `#restart` keeps them. **Accepted risk (user decision):** the artifact shares the MCP/bus server's origin, so lane-authored JS can same-origin `fetch` the unauthenticated MCP endpoints — a second loopback origin was explicitly declined to keep one port (see ADR-0002 amended). Server caps comment count/body/quote/outerHTML (`413`). 28 harness tests green (`cargo test`); `npm run check`, `cargo clippy`, `cargo fmt` clean. See `docs/149-artifact-inline-feedback.md`.
- **Custom-command background-work indicator (spec 145 refinement)** — Custom commands that do background work (`#review`, `#wiki`, `#recall`, `#handoff`/`#resume`, `#goal`) gave no on-screen cue of *what* was happening: the busy chip read a generic `running`, indistinguishable from an ordinary turn, and `#review`'s slow async prelude (git-diff collection) ran while the lane was still `idle`. Each command's turn is now tagged with `HarnessLane.activeSystemLabel` (a `label` arg threaded through a refactored `enqueueSystemPrompt` → `beginSystemTurn`/`dispatchTurn`) so the composer chip names the operation (`reviewing` / `saving to wiki` / `recalling wiki` / `writing handoff` / `resuming` / `setting goal`), drained peer turns read `handling peer`, and `composerStatusChip` cues `starting…` during a respawn. The label is kept across `busy`+`needs_permission` (a permission pause is the same turn) and cleared by `setLaneStatus` only on a true turn end. **Reviewed by peer lanes — Codex-1 (architecture/correctness: 3 blockers + 2 warnings) and Claude-2 (requirements-fit: 0 blockers + 5 warnings), all verified against source; the human chose to fix every finding in this round.** The blockers were latent lane-claim races the indicator exposed: (B1) `#review` now **reserves** the lane (`reserveCommandTurn` → `busy`) before the async collection so peer mail / another prompt can't claim it and make the send silently no-op — `releaseReservedTurn` on bail, `dispatchTurn` on success, reviewers re-validated for liveness first (W2); (B2) the label is preserved across `needs_permission` instead of dropping to `running` after an approval; (B3) `#goal`'s seed is reserve-aware — if drained peer mail claims the freshly respawned session the goal still applies to subsequent turns and the seed defers with a transcript note rather than dropping silently. Plus wording polish (`saving to wiki`, `setting goal`) and a corrected ordering comment (W1). Frontend-only, single file (`src/acp/acp-harness-view.ts`); no `types.ts`/MCP/Rust change beyond the `activeSystemLabel` field. 191 harness tests green. See `docs/145-harness-design-review-panel.md` › *Background-work indicator*.
- **Lane Goal — focus scope (spec 148)** — A per-lane `#goal <text>` composer command that declares the lane's current task as a **focus scope** — inspired by Claude Code's `/goal` but deliberately dropping its autonomy half (no completion-condition evaluator, no auto-continue): it borrows only the *name* and the *clear-on-new* behaviour. Setting a goal **clears the lane** (fresh ACP session + empty transcript via `newLaneSession`, memory preserved, peer inbox/pending untouched) and seeds the first turn with the goal embedded; the goal text then rides **this lane's own** turns via the per-turn `lane-context.md` packet (`renderPromptMemoryPacket` → `insertGoalLine`) so the agent stays anchored, and shows as a flat, static goal-bar above the composer meta row (`renderGoalBar`). `#goal` shows the active goal + age; `#goal clear` (aliases stop/off/none/reset) removes the scope **without** touching the session. Per-lane, session-only runtime state; survives `#new` (re-injected each turn), dropped on lane close. **Reviewed by peer lanes across two rounds — design (Codex-1: 3 blockers + 2 warnings) and code (Codex-1 architecture/correctness: 3 blockers + 2 warnings; Claude-2 requirements-fit: 0 blockers + 4 warnings), all verified against source.** The code-round blockers exposed that the original "route every programmatic turn through `buildPromptBlocks`" pin (so the goal rode peer/handoff turns too) entangled with shared inter-lane machinery — it bypassed the directive promote/consume lifecycle in `sendUserPrompt` and the `__harness__` seed envelope made `InterLaneCoordinator.drain()` clear cancelled-peer tombstones. The human resolved the surfaced fork by **narrowing the scope: inject the goal only into the lane that set it, never other lanes' turns.** Final shape: (B1) `#goal <text>` requires `idle`; the goal is published **after** a confirmed respawn (not before, closing the mid-respawn window where a peer turn could ride a stale goal); (B2) `enqueueSystemPrompt` is left **raw** (reverted) — programmatic/peer turns of all lanes are untouched; (B3) the first turn is seeded by a **direct, self-contained** `enqueueSystemPrompt(goalSeedPrompt(text))` to *this* lane only (no `injectHarnessEnvelope`, so no tombstone is cleared); the goal pin rides only this lane's normal turns via `insertGoalLine` (emitted in **both** return paths of `renderPromptMemoryPacket`, W4); (W1) `newLaneSession` returns `false` when the respawn errored so `#goal` doesn't falsely claim success; (W2) goal-bar age is a render-snapshot, not a live ticker (idle-CPU budget); plus copy/terminology polish (W5 "harness-lane runtime scope"; unified empty-state wording). Frontend-only, single file (`src/acp/acp-harness-view.ts`) + one CSS rule; no `types.ts` change beyond the local `LaneGoal`, no new MCP tools, no Rust. 191 harness tests green. See `docs/148-lane-goal-focus-scope.md`, `CONTEXT.md` › *Goal*.
- **`#review` simplified to agent-orchestrated multi-reviewer (spec 145)** — The structured Review Lane Mode (spec 112) was over-engineered for a keyboard tool with a human in the loop: a git `ReviewPacket` (worktree fingerprint, partial-staging detection, churn-sorted hunk caps), a rigid `review_reply` findings schema, dedicated `review_request`/`review_reply` MCP tools, a bespoke per-sender `inFlightReviews` delivery path, and a `ReviewCard` UI — and it reviewed **one** lane only. `#review` is now modelled on the `#wiki` pattern (spec 144): it collects the review *subject* — the working `git diff` **or** a design-doc file (`#review [<lane> …] [-- <docpath | note>]`) — and injects **one** prompt via `enqueueSystemPrompt` that directs the convening lane to `peer_send` the subject to **every** named reviewer **in one turn** (overriding the default "end your turn after one send"), then synthesize the replies, clustering concerns raised by ≥2 reviewers and routing genuine forks to the human via `attention_flag`. Reviewer set is **auto-detected** (bare `#review` = all other live local lanes; naming lanes is a subset override), with round-robin lenses (architecture / requirements-fit / simplicity) so one reviewer doesn't just share the author's blind spots. The net change is a large **deletion** (findings schema, `ReviewCard` + its CSS, both MCP tools + Rust handlers, the coordinator delivery path, `review` envelope kind/transcript kind, `reviewedThrough`/`reviewReplyAttemptsThisTurn` lane state) while **retaining a shared, simplified git-state collector** (`{ hasGitRepo, isUnbornHead, diffstat, diff, untracked }`, UTF-8-safe payload cap, untracked excerpts + unborn-HEAD handling) that attention triage (spec 128) still consumes for a flagged decision's blast-radius. Reviewed by peer lanes Codex-2 (architecture/correctness) + Cursor-1 (requirements-fit/simplicity); their findings (the `peer_send` "end your turn" fan-out risk, untracked/unborn-HEAD gaps, intent-in-prompt, skim template) are folded into the spec. Accepted residual: all-reviewer delivery + synthesis are model-driven best-effort, not harness-guaranteed (the B2 trade-off). See `docs/145-harness-design-review-panel.md` (supersedes the delivery/UI machinery of `docs/112`).
- **Code-wiki commands `#wiki` / `#recall` (spec 144)** — Two ACP-harness composer commands that maintain an **LLM-Wiki-style code wiki** (the persistent *why/decisions/domain* layer the code doesn't record) as plain markdown in the **target** repo at `<cwd>/docs/wiki/` — explicitly **not** the `krypton-harness-memory` store, which is a per-lane opaque blob outside the repo with no git/no human browsing (ADR-0003). Modelled exactly on `#handoff` (spec 139): both inject a **one-shot** prompt via `enqueueSystemPrompt` (no always-on stub, **zero per-turn cost**), guarded on `projectDir` (they touch repo files, not memory) + idle/`awaiting_peer` status, draft cleared before validation. `#wiki [hint]` distills the current conversation into the wiki — **incremental ingest with first-run bootstrap** (`index.md` catalog + `log.md`), preserving existing claims and keeping both sides of a conflict as an open question rather than clobbering. `#recall <question>` reads `index.md`, drills the smallest relevant set of pages following cross-links, and answers in-turn with path citations (read-only, prompt-enforced). The two prompt strings **are** the wiki "schema" (the discipline); they harden against prompt-injection (conversation/tool content treated as data; `hint`/`question` JSON.stringified + tagged) and broaden the secret rule to private/personal data, env values, and sensitive raw output (best-effort, not a hard boundary). Write/read kept as **distinct verbs** — different inputs, side effects, and safety, so no ambiguous `#wiki <text>` mode switch. Frontend-only, single file (`src/acp/acp-harness-view.ts`): two prompt builders, two `runHashCommand` branches, two help-drawer entries; no `types.ts`/lane-state change, no new MCP tools, no Rust. Reviewed by peer lane Codex-1 twice — design (7 findings folded: prompt-injection hardening, clobber prevention, bootstrap recovery, broadened secret rule, `#recall` cross-link traversal, guard-order/edge-case docs) and code (no blocking findings; folded the two non-blocking notes — softened an over-categorical injection comment, and exported the two pure builders to add focused unit tests; plus a no-fabrication clause for a first run with nothing settled). The prompt builders are now covered (8 tests: load-bearing clauses, empty-hint omission, JSON.stringify neutralization of injection-laden multiline input); 100 harness tests green. See `docs/144-harness-wiki-command.md`, `docs/adr/0003-code-wiki-lives-in-target-repo-not-harness-memory.md`.
- **peer_send autonomous delegation (spec 143)** — `peer_send` gained an optional `auto_accept` boolean so a lane can delegate work to a sibling and let the recipient's peer-injected turn run **without babysitting the permission gate** — but it auto-accepts everything **except high-risk commands**, which still prompt the human. The gate reuses the spec-140 `classifyBashCommand().highRisk` set (destructive verbs / dangerous git / script / network / unparseable) as the single source of truth, via an exported pure `permissionCommandIsHighRisk(call)`: it classifies the **untruncated** command (`extractCommandLineRaw`, split out from the 96-char display helper `extractCommandLine` so a destructive tail past the cutoff like `echo …<96> && rm -rf x` can't slip through — Codex-1 catch), and an execute-like surface whose command can't be read (kind `execute`, shell-ish rawName/title via a shared `SHELL_LIKE_PREFIX`) is treated as high-risk (unknown ⇒ high-risk). File-write reviews auto-accept (diff-shown + VCS-recoverable). A **dedicated** per-turn `lane.peerAutoAcceptForTurn` flag (not a reuse of the unconditional `acceptAllForTurn`) is armed in `enqueueSystemPrompt` when the drain context says so, surfaced by a `peer-auto` status chip + a system transcript line naming the granter, and reset at every turn-end path **and in the `enqueueSystemPrompt` catch** (so a `client.prompt()` throw can't strand the arm into a later manual turn). Honored only for **same-view sibling senders on a request/initiation envelope**: the drain arms `autoAcceptPermissions` only when *every* mail envelope in the composed turn is local (`getLane` non-null) and carries `auto_accept` and is not a reply — a foreign/cross-harness sender's `auto_accept` is coerced to `false` and reported back in the `peer_send` result (`auto_accept ignored: cross-view sender`), closing the confused-deputy / privilege-amplification paths. Reviewed adversarially by Codex-1 (REVISE → fs-write-review scope, mixed-batch amplification, catch leak, initiation-only) and Cursor-1 (ACCEPT w/ revisions → cross-view feedback, vocabulary); all folded in. The user chose blanket-except-high-risk over both blanket (Cursor) and full-classifier (Codex B3). Covered by `src/acp/inter-lane.peer.test.ts` + `src/acp/inter-lane.cross-view.test.ts`. See `docs/143-peer-send-auto-accept.md`.
- **Active-lane → window accent (spec 142)** — In the ACP harness view the host `.krypton-window` wore a fixed compositor-allocated color unrelated to its lanes. Now the window chrome (status dot, border, corner L-marks, edge glow, accent bar) tracks the **currently active ACP lane's identity color**. A private `applyActiveLaneAccent()` helper, called from `render()` (the single funnel all active-lane changes pass through — `activateLane`, `closeActiveLane`, initial mount — so no `activeLaneId` write bypasses it), sets `data-lane-accent="<slot 1–10>"` on the host window via `this.element.closest('.krypton-window')`; the slot is `((lane.index - 1) % 10) + 1` (from `lane.index`, never the self-referential slot-1 `lane.accent`). CSS in `window.css` maps each slot to `--krypton-window-accent` + `--krypton-window-accent-rgb` with **`!important`**, which beats the compositor's *normal* inline accent vars **without removing them** — so dropping the attribute (on no active lane, or in `dispose()` while the element is still attached) reverts the window to its allocated color with no restore path. The existing `data-signal` view-protocol override (spec 105) gets `!important` + later source order so a status signal still wins the `-rgb`-driven surfaces (border, glow, corner *glow*) transiently, while the solid-hex surfaces (dot, accent bar, corner *fill*) keep lane identity. Rejected the naive inline-style write (clobbers the signal layer) and the remove-inline plan (leaks the wrong base color when a pane disposes but its window survives, `compositor.ts:4008-4016`). Reviewed pre-implementation by Codex-1. See `docs/142-active-lane-window-accent.md`.
- **Cross-harness peering (spec 141)** — `peer_send`/`peer_list` reached only lanes in the *same* `AcpHarnessView`; a second harness view (another tab/DOM window) was invisible even on the same project. Lifted by two pieces. (1) **Globally-unique lane names**: lane `displayName` numbering moved from a per-view count to a process-wide, never-recycled monotonic counter keyed by the rendered label prefix (`nextLaneNumber('Claude')→1,2,3…`) in a new `HarnessDirectory` singleton (`src/acp/harness-directory.ts`) — so a bare `displayName` permanently identifies one lane across every view, dissolving the cross-view name collision at the source (no `@H#` handle, no generation token). (2) **In-process router**: each view registers a `HarnessEntry` (carrying `cwd` + `alive`) on start and unregisters on dispose; because all views share one JS runtime the cross-view hop never leaves JS. `InterLaneCoordinator.deliver()` was factored into `acceptInbound()` (recipient side — classifies initiator-vs-replier against the *target* coordinator's pending table, where that state lives, coerces a replier's `done`, pushes + drains) and `recordOutbound()` (sender side — outbound row + conditional `trackPending`); same-view `deliver()` calls both, the cross-view bridge calls them across two coordinators, keying pending by the foreign `displayName` so reply-clear matches on both legs. `#cancel` toward a foreign pending peer routes `acceptForeignCancellation` onto the target (notice + termination prompt + tombstone) so a late foreign reply is still dropped; a disposing view flips `alive=false` and fans `onForeignHarnessClosed` snapshots to peers. `peer_list` folds in `peersFor(harnessId)` tagged `local:false` with each peer's `cwd`. No project isolation (cross-project consultation is the goal; `cwd` is disclosed as context, not a wall) — except `review_request`/`review_reply`, which reject a foreign-`cwd` reviewer with `cross_project_review` (worktree fingerprint is per-repo). Tool descriptions (`hook_server.rs`, text-only) now flag the cross-project trust boundary. Covered by `src/acp/inter-lane.cross-view.test.ts`. See `docs/141-cross-harness-peering.md`.
- **Approval-gate safety (spec 140)** — The agent-view write/command approval gate let a single `Shift-A` silently arm `acceptAllWritesForTurn` / `acceptAllCommandsForTurn` for the rest of the turn, with no visible armed state, no disarm, and no severity gating (`rm` was classified the same `write` tier as `touch`). Three additions, modelled on Claude Code's auto-accept (always-visible mode, toggle off anytime, bash never blanket-approved): (1) a **persistent armed indicator** in the status line naming what is live (`⚠ AUTO-RUN FILES/CMDS/FILES+CMDS · esc`, or `AUTO-BLOCK`) — this required refactoring the status line into stable child segments (`[spinner][timer][armed][telemetry]`) since `renderStatusLine` set `.textContent` and was clobbering the prepended spinner/timer each `usage_update`; (2) **`Esc` disarms** any armed turn-wide flag without aborting the agent, as the first branch of `onKeyDown` so it fires in every state and even with no pending rows; (3) **high-risk gating** — `classifyBashCommand` (now exported, unit-tested in `tools.test.ts`) computes a 4th `highRisk` field (destructive verbs `rm`/`dd`/`mv`/`chmod`/…, dangerous git `reset --hard`/`clean`/`push --force`, redirection, and conservatively all `script`/`network`/`unknown`); high-risk commands are excluded from whole-turn accept (`A` falls back to single-accept with a hint) and from armed auto-run (`requestCommandApproval` re-prompts), guarded in the key handler, `resolveOldestCommandApproval`, and the request path. UI adds a worded risk chip (`HIGH/LOW · <risk>`, colour + text) and a distinct whole-turn affordance (`· [A]all [R]all`) replacing the 6px hint. Per option A (Cursor-1 reviewed), file writes + low-risk command bash (`touch`/`mkdir`) stay blanket-able by design — weaker than Claude Code's no-blanket-bash, but the indicator names exactly what is live. See `docs/140-approval-gate-safety.md`.
- **User-triggered memory handoff (spec 139)** — Cross-session handoff reuses the lane's existing `memory_set` document, but is **user-triggered rather than always-on**. An always-on convention (the original draft, where the lane-context stub nudged every session to `memory_get` on start and refresh `memory_set` at checkpoints — Codex-1 reviewed and endorsed a once-per-session `handoffResumeHinted` flag for it) was rejected: unlike the cheap always-on stub behaviors (attention-triage, peering), handoff pushes *proactive* memory read/write work, so the token cost should be paid only on explicit request. Two composer hash-commands, mirroring the `#new`/`#cancel`/`#mem` family in `runHashCommand()`, inject a one-shot instruction turn via `enqueueSystemPrompt()`: `#handoff` tells the active lane to write/refresh a resume-ready `memory_set` doc (what's done / state / next steps / open questions; reference by path; no secrets since `memory_set` is unredacted; overwrite, ≤ 8000 chars), and `#resume` tells it to `memory_get { lane: "<self>" }` and continue (start fresh if empty). Both guard like `#new` — a no-op `flashChip` when memory is unavailable or the lane is busy. Frontend-only: no `types.ts`/lane-state change, no new MCP tools, no Rust change, lane-context stub untouched; the resume-flag machinery from the always-on draft is moot under an explicit command. Surfaced in the help-drawer command list. See `docs/139-default-memory-handoff.md`.
- **Weight-dynamic attention gauge (spec 138)** — The workspace-footer attention chip (spec 128) was flat amber `N attention` — count only, weight-blind. It is now **weight-dynamic while staying motionless** (honoring the spec-128 / ADR-0001 static-by-design rule): the chip's colour tracks the **heaviest open reversibility tier** (cyan `reversible` → amber `costly` → red `irreversible`, reusing the triage-overlay palette) and a **pip strip** encodes the count (1–6 lit bars, label `6+` past the cap). `AttentionTriageStore.openItems()` is already sorted by reversibility descending, so element 0 is the max tier — no new computation. The `system:attention` ViewBus signal gained `maxReversibility: AttentionTier | null` (self-contained union in `view-bus-types.ts`); `acp-harness-view` publishes it (renamed `publishAttention`, dedupes on count **and** tier so a tier change at the same count still re-publishes); `WorkspaceFooter.attentionBySource` now holds `{count, tier}` per source, summing counts and max'ing tiers across harness tabs (one red item anywhere ⇒ red chip). No motion/pulse (rejected variant), no per-tier breakdown (only the max tier is carried). Visual chosen from an interactive prototype (variant D). See `docs/138-attention-gauge-weight-dynamic.md`.

- **ACP Harness agent image rendering** — agent-emitted markdown images now display in the transcript instead of broken-image boxes. Agents that generate images (e.g. Grok's `image_gen`, which writes a JPEG under `~/.grok/sessions/<enc-dir>/<id>/images/N.jpg` and emits `![alt](/abs/path.jpg)`) reference them by bare absolute filesystem path; the webview origin can't fetch raw FS paths, so they rendered as `?` placeholders. New `resolveLocalImageSrcs(root, cwd)` (next to the spec-117 `sanitizeSrc` helpers) walks `<img[src]>` in a rendered body and rewrites local sources to loadable asset URLs via `convertFileSrc()` (asset protocol already enabled in `tauri.conf.json` with `$HOME/**` scope) — a leading `/` is treated as a **true absolute path** (agent output), not cwd-root-relative as in the markdown viewer; relative paths resolve against the lane's project dir; remote/`data:`/already-resolved schemes are skipped so the pass is idempotent across re-renders. Wired at all three markdown→DOM sites: seal branch A (live foreground body, never re-rendered post-seal), seal branch B (offscreen background capture), and the `renderTranscriptItem` cold-load consumer (now threaded a `projectDir` param). A failed asset load swaps in a styled `IMG BREACH // <path>` notice; `.acp-harness__msg-body--markdown img` is capped to the column width. Verified the on-disk path + format with the live Grok-1 lane. See `docs/72-acp-harness-view.md`.

- **Markdown viewer — search, heading hints, image fix, focus indicator, re-select guard (spec 137)** — Five enhancements to the markdown viewer. (1) **In-doc search**: `/` in the preview opens a transient bottom HUD that wraps case-insensitive matches in `<mark>` (skipping `pre`/`code`, capped at 500), `n`/`N` (or `Enter`/`Shift+Enter`) cycle with a neon flash + `pos/total` counter, `Esc`/`q` clears and unwraps. (2) **Heading hints**: `H` reuses the link-hint label engine over `h1`–`h6` to jump+scroll — the engine was generalized to `enterHintMode(targets, onPick)` shared by links (`o`/`;`) and headings. (3) **Image fix**: relative `<img src>` is resolved against the current file's directory and loaded via `convertFileSrc()` (asset protocol enabled in `tauri.conf.json` with a `$HOME/**` scope — raw `file://` is blocked cross-origin); a failed load swaps in a styled `IMG BREACH // <path>` notice. (4) **Focus indicator**: the focused panel now shows a 3px glowing accent edge bar and a brightened, glowing header while the unfocused header dims — pure CSS on the existing `--panel--focused` class. (5) **Re-select guard**: `loadFile` early-returns when the file is already shown (re-selecting just moves focus to the preview without re-fetch/re-animate/scroll-reset); `r` passes `force` to still re-read from disk. Designed with peer input from Grok-1. See `docs/137-markdown-viewer-search-hints-images.md`.

- **ACP Harness prompt queue (spec 136)** — Typing a prompt while a lane is busy no longer flashes "lane busy" and discards it. `submitActiveLane` now captures the prompt into a per-lane FIFO `queuedPrompts` (text + a frozen `StagedImage` snapshot + mention targets resolved at enqueue via `parseMentionFanOut`), capped at `PROMPT_QUEUE_MAX = 10`. The agent-dispatch back-half was extracted into `sendUserPrompt(lane, text, images, opts)` — shared by the immediate submit and the queued drain; it never clears the live draft, so draining a queued prompt can't wipe what the user is typing (the immediate caller clears its own draft). `finishTurn` resets the turn pointers **before** its draining status transition (prerequisite fix: `setLaneStatus(idle)` synchronously drains peer mail re-entrantly, and the old tail-cleanup clobbered that new turn's state), then schedules `maybeDrainPromptQueue` via `queueMicrotask` to read the settled status — **peer mail wins** (it flips the lane back to busy first; the user queue holds, shown as `held behind lane mail`). A consumed-but-undelivered drain (a queued `@mention` whose target closed before drain) emits a `queued prompt not sent` row **and re-arms the drain** so the rest of the queue doesn't stall. Per-item keyboard management via `#unqueue [N]` / `#queue clear` / `#queue edit N` (strict `/^[1-9]\d*$/` index, command text consumed on every branch, `edit` guards staged images not draft text, re-sends to the tail); `#cancel`/`Ctrl+C` still clears the queue. The queue renders in a new bottom-anchored right-rail slot (`data-slot="queue"`, `flex-shrink:0` + capped height so plan/peek absorb the squeeze) — numbered drain-order rows, `▸` next marker, `→lane`/`img×N` tags — shown independently of the lane peek, hidden in zen; the composer chip appends `· N queued`. Reviewed adversarially by Codex-1 (×2), Grok-1, Claude-2 — 22 findings, all adopted. See `docs/136-acp-harness-prompt-queue.md`.
- **ACP Harness Grok lane (spec 135)** — xAI Grok Build is now a built-in ACP backend. The lane picker lists `Grok`, spawning `grok agent stdio` (native ACP over stdio JSON-RPC) with cached login-shell environment injection. Grok is a regular permission-gated lane: the project `.mcp.json` bridge (spec 83) and the per-lane `krypton-harness-memory` MCP server both apply, capability-gated by what Grok advertises at `initialize`. Touchpoints follow the Droid template (`builtin_backends()`, `BUILTIN_BACKEND_IDS`, `BACKEND_LABELS`, `backendLogoId` + a new `krypton-logo-grok` bolt glyph, `laneAccentForLabel`) plus two net-new bits the 10th lane forces: a 10th `laneAccent` color (so `laneAccentForLabel('Grok-1')` doesn't modulo-wrap onto Codex blue) and a `--krypton-backend-grok` CSS token + `--grok` rail/directive tint classes. Grok-specific startup diagnostics cover missing installs, old CLIs without ACP, auth failures, API-key issues, and empty-stderr first-run stalls. **Model selection is conservative in v1**: no spawn-time `-m` flag (unverified under `agent stdio`); `acp_harness.lane_models.grok.active` applies through the existing generic `session/set_model` path if Grok advertises model state. Two verify-at-implementation items remain (does `agent stdio` accept `-m`; does Grok honor `session/new mcpServers` or regress like Cursor) — both have defined fallbacks and neither blocks registration. Peer-reviewed by Codex-1 (6 findings, all adopted after independent verification). See `docs/135-acp-grok-lane.md`.
- **ACP Harness artifact default styling (spec 134)** — `artifact_new` now seeds the issued path with a self-contained styled scaffold instead of an empty file, so artifact output has a consistent baseline the lane fills rather than authors from scratch. The scaffold (`src-tauri/resources/artifact-scaffold.html`, embedded via `include_str!`) defaults to the **Krypton cyberpunk theme** (dark, monospace, dense — user-chosen for being minimal/space-efficient) with an in-page **cyberpunk → light → auto** toggle persisted best-effort to `localStorage`; only `auto` follows `prefers-color-scheme`. Inline CSS (no external stylesheet to resolve over `file://`, no harness mutation of HTML at register) keeps the spec-133 path-handoff invariant intact — the harness authors the *starter* file before handoff, never the lane's later content. Written atomically (temp+rename, fail-closed); the title is HTML-escaped in. `artifact_new` returns a `content_marker` and its tool description + the lane-context stub now say "the file already exists — **edit**, don't recreate" to make the styled default stick (a strong default, not enforced — a lane can still overwrite it, and registering the untouched scaffold is allowed). No config key (cyberpunk default + toggle only). See `docs/134-artifact-default-styling.md`.
- **ACP Harness HTML artifacts (spec 133)** — A project-agnostic harness primitive letting a lane hand the user a rich, iteratively-editable HTML view. Path-handoff: a lane calls `artifact_new { title }` on `krypton-harness-bus`, the Rust hook server allocates an id and issues a destination path at `<cwd>/.krypton/artifacts/<harnessId>/<laneLabel>/<id>.html` (creating a self-ignoring `.krypton/artifacts/.gitignore`, failing closed if it can't), the lane writes the file with its normal file tool, then calls `artifact_register { id }`. Rust validates the file (rejects symlinks in any path component, hardlinks, non-regular files, wrong basename, over-cap size) and records size+sha256; `AcpHarnessView` raises a hintable card. The HTML never travels through MCP, and artifact-path write/edit cards are **redacted to path+bytes+hash** so HTML never enters the transcript model (the real spec-103 fix). Issued-path write auto-approval is a **separate** path/registry-keyed mechanism (not the spec-96 server-marker detector) keyed on path + issued-id + same lane. The card opens in the OS browser (ADR 0002) via `open_url(file://…)` from a per-card "open artifact" hint (`f` in command mode → label). Artifacts are live editable files: the harness re-stats on each observed edit; pending artifacts carry a write grant and are auto-cancelled at turn-end; everything is swept on harness close + a startup stale-sweep. Opt-in only (conservative-propensity guard in the tool description + lane-context stub). See `docs/133-harness-html-artifacts.md`, `docs/adr/0002-html-artifacts-open-in-os-browser.md`, and `docs/prototypes/133-harness-html-artifacts.html`.
- **Krypton brand glyph (spec 132)** — The app's first own-identity mark. A monoline "K" (solid cursor stem-bar + command-prompt chevron, the "Stem-bar" candidate from prototype 132) is inlined as a static SVG anchor at the leading edge of the global `WorkspaceFooter`, before the mode segment. It is created once in the constructor and never re-rendered (`renderLeft/Center/Right` only touch their own cells), so it stays off the refresh hot path; the responsive media queries hide data segments by class and leave the brand untouched. Tinted via the footer's existing `--krypton-window-accent-rgb` accent var at 0.85 with a tight+bloom `drop-shadow` glow stack, so it recolors with the theme for free. The harness empty state was evaluated first and rejected — the harness auto-adds a default Claude lane on start, making that surface effectively dead. No raster assets, no new chrome, no Rust changes. See `docs/132-krypton-brand-glyph.md` and `docs/prototypes/132-krypton-brand-glyph.html`.
- **ACP Harness default-on attention triage (spec 130)** — `attention_flag` / `attention_resolve` are now advertised to every lane that receives the `krypton-harness-memory` MCP server, removing the manual/directive equip requirement and the `tools/list` race for newly spawned lanes. The Rust hook server always includes attention descriptors and no longer rejects attention calls based on the legacy triage-equipped map; payload validation and frontend queue insertion remain unchanged. `AcpHarnessView` seeds every lane into `AttentionTriageStore` at creation so silent-turn audit starts from the first response, and `Cmd+P → '` has been removed from the active harness leader keys. `[[directives]].triage_equipped` remains accepted and visible as a legacy badge for old directive files, but no longer grants unique capability or triggers same-lane assignment approval. See `docs/130-default-attention-triage.md`.
- **ACP Harness directive-bound triage grant (spec 129)** — Made the spec-128 triage equip sourceable from a directive instead of only the manual `Leader '` toggle, which solves the "equip mid-session does nothing" trap (ACP clients fetch `tools/list` once at session start). A directive (`HarnessDirective`, `acp-harness.toml`) gains `triage_equipped: bool` (default `false`); a lane *spawned* with such a directive (`addLaneFromDirective`) is equipped **before** `spawnLane` starts the ACP client — the Rust `acp_set_lane_triage_equipped` mirror is now **awaited** ahead of spawn (not fire-and-forget) so the tool is advertised in the lane's first and only `tools/list` fetch. `HarnessLane.triageEquipped` is now the *effective* state, derived: manual override (`triageOverride`) wins, else the bound directive's grant. Assigning a directive clears the override; a disabled/deleted directive drops its directive-sourced grant on reload (`refreshDirectives`) while a manual override survives. The directive approval card, picker rows, and composer chip surface the grant (`◆ triage` + source) so a capability grant is never buried in TOML. Assigning a triage-granting directive is treated as a capability escalation: it requires explicit user approval even **same-lane** (which `directive_apply assign` otherwise auto-approves), closing a hole where a lane could self-equip the flag capability without confirmation — approving a directive's *existence* never pre-approves a given lane equipping from it. The three existing gates (tools/list filter, call-time gate, `mergeAttentionFlag`) are untouched. Boolean over a general tool-grant list (deferred until ≥2 gated tools); mid-session reassign updates call-time auth + UI but not tool advertisement (documented caveat — a real fix needs MCP `tools/list_changed`). Reviewed adversarially by a peer lane (7 findings, all adopted). Superseded for tool visibility by spec 130; see `docs/129-directive-triage-grant.md` and `docs/130-default-attention-triage.md`.
- **ACP Harness attention triage (spec 128)** — A per-lane opt-in router that surfaces the *decisions that need the human*, not lane activity. An equipped lane self-reports at end-of-turn via a new `attention_flag` MCP tool (`question` / `chosen` / `rationale` / `traded_off` / `uncertainty` / `reversibility`); the harness validates a presence floor (non-empty `traded_off`, non-blank `uncertainty`), assembles git blast-radius (diffstat) like a review packet, and inserts a `JudgementItem` into a **demand queue** (`AttentionTriageStore`) ranked by reversibility (irreversible > costly > reversible, ties oldest-first — `lane peek heat` is deliberately not consulted). The only ambient signal is a static **backpressure gauge** (`◆ N`) in the command center, hidden at zero. `Leader ;` summons a summon-on-demand overlay that shows one ranked card at a time, full-width (always shows traded-off + uncertainty, never collapsed; header shows the `2 / 5` cursor position); `j`/`k` page prev/next through the queue, `a` approves (bookkeeping only — no lane effect), `r` redirects (one-line input → `coordinator.deliverRedirect` injects on the lane's next idle), `o`/`Enter` digs into the lane transcript, `Esc` closes. A lane's `attention_resolve` demotes its own item to the **silent pile** (never deleted; terminal human status wins). Per-equipped-lane `LaneTriageStats` track flagged-vs-silent turns (busy→idle) for the ADR-0001 audit, shown in each card header. Equip is a runtime per-lane toggle (`Leader '`), mirrored into the hook server (`acp_set_lane_triage_equipped`) so `tools/list` advertises the tools only to equipped lanes; the call-time dispatch re-checks and is authoritative. Non-blocking, summon-on-demand, no auto-approval. Superseded for tool exposure by spec 130; see `docs/128-attention-triage.md` and `docs/130-default-attention-triage.md`.
- **ACP Harness in-harness model picker (spec 127)** — Follow-up to spec 126: a keyboard-driven model picker for the focused lane, opened with the harness leader key then `,` (`⌘P` then `,`; `j`/`k` move, `↵` switch, `esc` cancel). `acp_session_new` (and now `acp_session_restore`, which previously discarded its whole response) surface the agent-advertised `availableModels` + `currentModelId` from `session/new` into `AgentSessionInfo` → `HarnessLane`; `apply_session_model` returns the applied canonical id so the picker's `✓` marks the running model, not the stale pre-switch default. A new `acp_set_lane_model` command switches the **live** lane via `session/set_model` (10s timeout) — rejected ids return `Err` (frontend reverts the optimistic chip), a timeout returns `Ok(TimedOutUncertain)` (kept + flagged, not reverted, since the agent may still apply it). Concurrent switches are guarded by a per-lane `modelSwitchEpoch` + `pendingModelSwitch` token so a slow/failed switch can't clobber a newer one. The `mode_update` handler now re-renders the lane header and, while a switch token is live, notes the adapter's mode downgrade (e.g. `auto` → `default` on Haiku) in the transcript. **Session-scoped only**: the picker does not rewrite `krypton.toml` (config still governs the spawn default; the choice resets on respawn). Disabled for backends that advertise no model state. See `docs/127-acp-lane-model-picker.md`.
- **ACP Harness lane model selection** — `acp_harness.lane_models.<backend>.active` now actually takes effect for ACP-native backends that advertise model state, starting with Claude. `acp_session_new` captures the `session/new` `models` object and `apply_session_model()` (awaited inline, before the first prompt can race it) keeps OpenCode on its dedicated `set_config_option`-first path, then for any other backend gates on a valid session model state (an `availableModels` array or `currentModelId` string) and sends `session/set_model {sessionId, modelId}`. The configured value is sent **verbatim** so aliases (`opus`/`sonnet`/`haiku`) resolve adapter-side; failure is **non-fatal** (lane keeps the agent default) and surfaces as `model_apply_failed` → an amber `⚠` warning on the lane model chip with an explanatory tooltip. No new config schema; `active` stays per-backend and is snapshotted at spawn (respawn-to-apply — editing `krypton.toml` re-applies only on next spawn/`#new`/`#new!`/restart). Gemini/Droid spawn-flag and OpenCode paths unchanged; Cursor/Junie/OMP/Pi still skip (no advertised model state) and auto-enable if their adapter gains it. Resume/load sessions keep their saved model. See `docs/126-acp-lane-model-selection.md`.
- **ACP Harness directive_remove MCP tool** — Added a dedicated `directive_remove({ directive_id, reason })` bus tool on `krypton-harness-bus` as an alias for `directive_apply({ action: "delete" })`. Both share the same approval gate, frontend round-trip, and `{ action: "delete", approval, deleted }` return shape. See `docs/124-acp-harness-directive-management.md`.
- **ACP Harness lane-rail disambiguation** — Zen-mode lane rail entries now read as `dot · backend logo · head · meta` instead of `dot · head · meta` with a user-icon. The backend logo is an inline-SVG monogram tinted per `backendId` (nine `<symbol>` defs injected once on view mount, fall back to a neutral OMP mark for unknown ids). The meta line replaces `directive.icon` with a role tag chip whose label and color are derived from `directive.task`: matched keywords land in canonical roles (`analysis` / `review` / `impl` / `plan` / `explore`); unmatched non-empty values display the raw lowercased `task` and pick a stable djb2 hash-bucket color; empty `task` displays the literal `custom`. The directive title is trimmed of a single leading `<BackendLabel> ` prefix for display only — storage, the picker, and `peer_list` keep the full title. Pending-clear moves from the retired `acp-harness__rail-meta__icon--clearing` to a new `acp-harness__rail-tag--clearing` modifier (line-through + dashed border + dimmed). `turnDirectiveOverride` is intentionally not previewed in the rail. No schema, MCP, or Rust changes. Inline SVG monograms are placeholders (geometric, not licensed brand assets); v1 ships as-is. See `docs/125-lane-rail-disambiguation.md`.
- **ACP Harness directive management** — Reusable, backend/task-scoped **directives** (system-style prompt blocks) now live in a Krypton-owned `~/.config/krypton/acp-harness.toml` (created empty on first harness open; atomically rewritten on approved mutations). A new `acp_harness_config` Rust module loads/validates/saves directives (hand-written `Default` impls, kebab-case ids, 16 KiB prompt cap). `Cmd+P → .` opens a keyboard directive picker for the focused lane (`/`/`R` were unavailable — every letter is a reserved global leader key and `/` `;` `?` are taken); a composer `directive …` chip mirrors the binding. The active directive injects into the **same leading context packet** as the lane-context stub (never a second block). Three bus MCP tools sit next to `peer_send` on `krypton-harness-bus`: `directive_list`/`directive_preview` answer from Rust; `directive_apply` blocks on a frontend round-trip (the `peer_send` pattern). Persistent `upsert`/`delete` and cross-lane `assign` are gated by an `a`/`r` approval card in the lane transcript (upsert shows a before/after `system_prompt` diff); same-lane `assign` auto-approves. This is a real gate independent of backend permission behavior, because MCP tool calls otherwise execute immediately in Rust. See `docs/124-acp-harness-directive-management.md`.
- **ACP provider error rendering** — ACP Harness assistant streams now classify short provider/API failures such as `resource_exhausted`, 429/rate-limit, quota, auth, context, network, and overload errors when the assistant row seals. Matched rows are rewritten into structured `provider_error` cards with a headline, hint, retryability chip, and collapsed raw details instead of being markdown-rendered as malformed assistant prose. Structured ACP errors and prompt failures also use the same classifier when possible; unclassified errors stay as system rows. See `docs/123-acp-provider-error-rendering.md`.
- **Quick File Search Helix/input isolation** — Editor tabs opened from Quick File Search still wire xterm input before `spawn_pty`, but no longer flush pre-session xterm.js capability replies into the new `hx` process. Shell panes keep the existing startup reply buffering; direct Helix tabs discard those early bytes so markdown/file search opens cannot inject stray terminal responses as editor input. Quick Search result hover no longer changes selection, preventing pointer movement from racing keyboard navigation; explicit row clicks still accept the row. See `docs/68-quick-file-search.md`.
- **ACP Harness OMP lane** — Oh My Pi is now a built-in ACP backend. The lane picker lists `OMP`, spawning `omp acp` with cached login-shell environment injection. OMP accepts `session/new mcpServers` but also native-loads root `.mcp.json`, so Krypton skips the project `.mcp.json` bridge for OMP while still injecting the per-lane `krypton-harness-memory` MCP server. OMP-specific startup diagnostics cover missing CLI installs, old CLIs without native ACP mode, explicit auth failures, API key issues, and empty-stderr first-run/auth-broker stalls. OMP lanes use the normal permission rail and do not show a warning chip in v1. See `docs/122-acp-omp-lane.md`.
- **WorkspaceFooter status rail** — The old music mini-player no longer owns the full bottom strip. A new `WorkspaceFooter` owns the single fixed 28px workspace rail and renders mode, focused role/title, CWD/git, counts, focused-view process/activity/progress, and contextual hints from `InputRouter`, `Compositor`, existing `ViewBus` signals, and debounced git probes. Music registers as a bounded right-side segment with track/time/progress/mini-visualizer state. `Leader ?` toggles compact/detail density, and command palette actions can toggle detail or visibility. No new `view:status` protocol signal was added. See `docs/121-workspace-status-bar.md`.
- **ACP Harness Junie auto-allow gap** — `harnessAutoAllowToolName` now also scans `permission.options[].name`, so adapters (Junie/JetBrains) that emit `toolCall.title: "Allow running MCP?"` and surface the server/tool identity only inside option labels like `Always allow ("krypton-harness-memory:peer_send")` auto-approve. The built-in-server-marker + allowed-tool-name AND rule is preserved (third-party servers or non-allowed tools still prompt). Removed the temporary `[perm-debug]` warn/transcript dump in `addPermission()`. See `docs/96-acp-built-in-memory-auto-approval.md`.
- **ACP Harness Junie MCP overlay** — Junie ignores `session/new` MCP injection; Krypton writes per-lane `.junie/mcp/mcp.json` under `~/.config/krypton/runtime/junie/<harness>/<lane>/` and spawns with `--mcp-location <overlayRoot>`. Harness memory + `.mcp.json` bridge use Junie's native loader. See `docs/119-acp-junie-lane.md`.
- **ACP Harness Junie lane** — JetBrains Junie CLI is now a built-in ACP backend. The lane picker lists `Junie`, spawning `junie --acp true` as a regular lane with the shared `.mcp.json` bridge and per-lane `krypton-harness-memory` server enabled. Junie-specific startup diagnostics cover missing CLI, unknown `--acp` flag (CLI predates ACP mode), explicit auth failures, API key issues, subscription/quota errors, corrupt `~/.junie/config.json`, and empty-stderr timeouts. Junie lanes show `⚠ permissions unverified` until ACP write-permission behavior is manually probed. The `laneAccent` palette grew from 7 to 8 colors to give Junie a distinct slot (avoiding the wrap-to-Codex collision). Auth: `junie` first-run (JetBrains Account), `JUNIE_API_KEY`, `-a/--auth <token>`, or BYOK provider keys forwarded through the cached login env. See `docs/119-acp-junie-lane.md`.
- **ACP peer activity UI** — Zen-mode lane rail now surfaces peer state (`⇆` awaiting, `▼N` inbox, `←`/`→` recent traffic) on every lane row; the active composer shows an informational peer strip (`#cancel drops pending peer wait`) without blocking input (spec 116). A direct peer peek candidate clears a prior `Esc`-dismiss only when the peer event arrived **after** the dismissal, so hiding the peek stays sticky while an unchanged peer candidate sits in the snapshot. See `docs/118-acp-peer-activity-ui.md`.
- **ACP Harness streaming flicker fix (rev 4)** — Mid-stream flicker was caused by alternating plain `textContent` and throttled `marked` `innerHTML` (structural snap every ~400ms) plus rebuilding lane chrome on every chunk. Streaming assistant/thought/user rows now append deltas into a single `TextNode` (`streamPlainLength`) with `--stream-plain` until `sealStreaming()`; markdown parses once on the next full render. Text chunks call `scheduleStreamingBodyOnly()` (transcript + one `applyStickyScroll()` only). Sticky scroll uses a single RAF while any lane is streaming. Fast path covers thought/user as well as assistant. See `docs/114-acp-harness-streaming-perf.md`.
- **ACP Harness streaming performance (rev 1–3)** — Prior work: RAF coalescing, stable `'stream'` signature, `activeToolCount` delta, scroll handler RAF gate, lane-body `contain: layout paint`. Superseded for flicker by rev 4 above.
- **ACP Cursor lane MCP via native `.cursor/mcp.json`** — `cursor-agent` was found to silently ignore MCP servers passed through ACP `session/new` (stdio and http), so the Cursor lane had no `peer_send`/`peer_list`/`memory_*` tools and could not message other lanes. Version-bisecting retained builds confirmed an upstream regression (`2026.05.20` worked; `2026.05.27`/`2026.05.28` do not), echoed by [Zed #50924](https://github.com/zed-industries/zed/issues/50924) and Cursor forum reports; `--approve-mcps` has no effect in ACP mode. Fix (mirrors the Junie overlay): Cursor is excluded from `session/new` injection; on spawn Krypton writes the per-lane `krypton-harness-memory` server into `<projectDir>/.cursor/mcp.json` (merging, preserving user entries) and pre-approves it with `cursor-agent mcp enable`, removing the entry on lane close. New Tauri commands `prepare_cursor_mcp` / `cleanup_cursor_mcp`; the `--approve-mcps acp` spawn arg reverts to plain `acp`. Verified end-to-end against the regressed `2026.05.28` build (peer_send callable). Project `.mcp.json` bridge servers are not yet mirrored for Cursor (memory server only). Tracked at wk-j/krypton#1. See `docs/113-acp-cursor-lane.md` (Revision 2026-05-29).
- **ACP Cursor lane** — Cursor Agent is now a built-in ACP Harness backend. The lane picker lists `Cursor`, spawning `cursor-agent acp` as a regular lane with the shared `.mcp.json` bridge and per-lane `krypton-harness-memory` server enabled. Cursor-specific startup diagnostics now cover missing CLI installs, Keychain credential lookup failures, and first-run/auth paths that need `cursor-agent login` or `CURSOR_API_KEY` outside Krypton. Cursor uses the normal permission rail; the v1 `permissions unverified` chip was removed 2026-07 (not probe-verified — see `docs/113-acp-cursor-lane.md`). Cursor model config is accepted for the lane chip but is not passed as a spawn flag until `cursor-agent acp --model <id>` is verified for ACP sessions. See `docs/113-acp-cursor-lane.md`.
- **ACP Review Lane Mode (V0.5)** — Lanes can now request a review of their uncommitted work from another lane via the `#review <lane>` chat command. The harness assembles a packet from `git status --porcelain` + `git diff HEAD --numstat` + per-file capped hunks + untracked excerpts + staging metadata (`hasStagedChanges`/`hasUnstagedChanges`/`partialStagingDetected`) + `worktreeFingerprint` (sha256 of HEAD + porcelain + changed-path size/mtime) + transcript-derived intent/commands/tool summary. Reviewers should reply via the new `review_reply` MCP tool, but delivery is best-effort: valid findings are kept, malformed findings are omitted with a summary note, and no-tool or no-finding replies render as clean review cards instead of protocol-blocking. Reply rendering recomputes the fingerprint and shows a `worktree changed since review request` banner when it diverges. Replies with findings are also injected into the requester lane as a model-visible follow-up turn so the requester can act on the review, not just display the card. Synchronous in-flight guard prevents concurrent duplicate requests; `cancelledPacketIds` set discards stale replies after `#cancel`. New `review_request` / `review_reply` MCP tools sit next to `peer_send` on the `krypton-harness-bus` server, auto-allowed alongside memory/peer. See `docs/112-acp-review-lane-mode.md`.
- **ACP harness right rail** — Active-lane plan strip and contextual lane peek no longer overlap. Both now live inside a single `.acp-harness__lane-rail` overlay container anchored top-right of the active lane, stacking vertically via flex column with explicit slot caps (`--primary` / default / `--compact`). The rail is an overlay (`position: absolute`, `pointer-events: none` with `auto` on slots) — it does not reserve transcript width. Plan element is reparented from the harness body into the rail's plan slot; peek is rendered into the rail's peek slot. The legacy `@media (max-width: 560px)` rule hiding peek at narrow widths is removed; the rail container layout absorbs that case. Metrics overlay stays floating (out of scope for v1). See `docs/111-harness-right-rail.md`.
- **Context-aware command palette** — `Cmd+Shift+P` now opens with a "Context" section pinned above the static list. Sourced from the focused pane's `ContentView` via an optional `getPaletteActions?(ctx)` capability. The ACP harness is the v1 contributor and exposes lane-status-conditional actions (cancel, restart, switch lane, open memory drawer) with keybinding hints. Static list ordering and fuzzy match are unchanged when the user types; the section header is hidden while filtering. Sync throws inside palette `execute()` are now caught. See `docs/110-context-aware-command-palette.md`.
- **ACP contextual lane peek** — The ACP harness shows a hideable, non-blocking peek for one automatically selected non-active lane. Candidate ranking is lane-local and context-driven: direct peer waits/messages first, then related permissions, non-active errors/permissions/inbox, then recent meaningful activity. The peek shows only lane name, status, reason, and one compact payload; it never becomes a global mixed-lane overview or mini-transcript. Command palette actions can show/hide, cycle, unlock, or activate the peeked lane, and `Esc` hides it only when a permission prompt does not own `Esc`. **Slice 109 heat:** optional lane-pair activity strip (active vs peeked) with palette metric/window/detail controls and click-through on the strip; see `docs/109-acp-contextual-lane-peek.md`.
- **ACP Harness transcript readability** — Permission rows now render as structured cards instead of string-only transcript lines. The harness splits built-in MCP auto-allow taxonomy into memory tools (`memory_set/get/list`) and peer tools (`peer_send/list`), while keeping both auto-allowed under the user-directed harness policy. Auto-allowed cards show the matched rule (`matched harness memory/peer auto-allow rule`), manual decisions mutate the original permission card in place, and failed permission replies mark the same card failed before logging the error. `awaiting_peer` lane activity now names the pending peer with coarse age buckets (`<1m`, `1m+`, `5m+`, `15m+`) and `#cancel` guidance without adding a persistent timer. See `docs/107-acp-harness-transcript-readability.md`.
- **ACP Harness Peering (inter-lane messaging)** — Lanes in the same harness can now hold user-directed peer-review conversations. New primitives: `LaneBus` (status event emitter), `LaneInbox` (per-lane FIFO), `InterLaneCoordinator` (drain-on-idle, awaiting_peer transitions). MCP tools `peer_send` + `peer_list` on the renamed `krypton-harness-bus` MCP server (auto-allowed alongside memory tools). New lane status `awaiting_peer` blocks the composer with pending-peer guidance and `#cancel`. No watchdogs, no budgets, no config — user controls lifecycle via `#cancel`. See `docs/106-inter-lane-messaging.md`.
- **ACP Harness transcript window** — Long sessions no longer pay a full O(transcript) render cost every frame. `AcpHarnessView.renderActiveTranscript()` now slices `lane.transcript` to a tail window (default 60 rows) and the existing diff cleanup loop removes older rows from the DOM. A muted indicator row at the top of the transcript shows the hidden count (`↑ N earlier rows hidden — Ctrl+H show 60 more`). `Ctrl+H` grows each lane's window by 60 rows; when the window reaches the total it snaps to "all" and the next press wraps back to 60. The setting is per-lane, resets on `#new`/`#new!`/`#restart`, and is not persisted across restarts. Pure frontend — no agent context impact. See `docs/103-acp-harness-transcript-window.md`.
- **In-app webview panes** — A new `webview` content type joins terminal/agent/vault as a first-class pane. External URL clicks from vault notes, markdown viewer, and the `f` hint mode now open as an in-app browser tab with cyberpunk chrome (address bar, back/forward/reload, loading bar) surrounding a native Tauri v2 child webview. Bridge script in the child page forwards leader chords (`Cmd+P`, `Cmd+L`, `Cmd+R`, `Cmd+[`/`]`, `Cmd+1..9`, `Cmd+W`) back to the host so the compositor stays reachable. Shift-clicking a link keeps the old "open in system browser" behavior. See `docs/102-webview-windows.md`.
- **Agent `/check` command** — AgentView now has a user-owned validation loop. `/check` detects the narrowest project command from marker files (`package.json` scripts `check`/`typecheck`/`test`, `Cargo.toml`, `go.mod`), runs it directly through `run_command`, renders output inline, and stores failing output so `f` or `/fixcheck` sends command + output back to the agent as a follow-up prompt. See `docs/101-agent-check-command.md` and `docs/42-pi-agent-integration.md`.
- **Agent bash approval** — The embedded pi-agent `bash` tool now classifies commands before execution. A small read-only allowlist runs immediately; redirection/heredocs, known mutators, Git state changes, package/network tools, script runners, and unknown commands render an inline COMMAND REVIEW row with risk/reason/cwd and wait for `a`/`r`/`A`/`R`. Rejected commands do not execute and return a tool error. See `docs/100-agent-bash-approval.md` and `docs/42-pi-agent-integration.md`.
- **Agent write approval** — The embedded pi-agent view now gates `write_file` before disk mutation. The tool reads old content, computes the existing inline diff preview when under the size cap, waits for AgentView approval, and only writes after `a`/`A`; `r`/`R` rejects without touching disk and returns a tool error to the model. `Ctrl+C` rejects pending writes before aborting. See `docs/99-agent-write-approval.md` and `docs/42-pi-agent-integration.md`.
- **ACP Harness rich tool output** — Execute-tool rows now keep a compact command header but render recognized Git outputs as structured console widgets: unified `git diff` output becomes compact file/hunk/add/delete rows, `git diff --stat` becomes per-file mini change bars, and `git status --short` becomes status badges with paths. Generic stdout/stderr remains available as labeled monospace sections with reduced glow so command headers stay readable. See `docs/72-acp-harness-view.md`.
- **ACP Harness memory degradation** — ACP Harness startup now treats the global hook server/memory MCP endpoint as optional. If `create_harness_memory` fails, the view records the startup error as a warning, still lists ACP backends, still spawns lanes, omits the `krypton-harness-memory` MCP descriptor, shows `memory off` in the composer and memory drawer, and has `#mcp` print the concrete startup error. See `docs/72-acp-harness-view.md` and `docs/73-acp-harness-mcp-memory.md`.
- **Hook server startup diagnostics** — The hook server state now records the concrete startup failure, including fixed-port bind errors such as `Failed to bind hook server on 127.0.0.1:64732: ...`. ACP Harness memory creation and hook-config snippet requests report that saved reason instead of the generic "Krypton hook server is not running" message when the port never became available. See `docs/72-acp-harness-view.md`.
- **Agent view compact tool labels** — Tool-row indicators such as `< BASH >` now use chrome-sized typography and tighter header spacing, while command text/output remains at the normal content font size. See `docs/42-pi-agent-integration.md`.
- **Agent view static cursors** — The pi agent view keeps the visible streaming and input block cursors, but removes their blink animations by deleting the cursor keyframes and animation declarations. See `docs/42-pi-agent-integration.md`.
- **File manager selected row simplified** — The file manager keeps a plain selected-row background for keyboard navigation, but removes the cursor chrome around it: no leading pipe, `>` marker, glow animation, shifted directory icon, cursor-specific type-color overrides, or boosted age/size styling. See `docs/52-file-manager.md`.
- **File manager 333px sidebar** — The file manager now starts with a fixed 333px file-list pane and lets the preview fill the remaining width. Local `H` / `L` resizing stays pixel-based from that baseline, using roughly 5% body-width increments with the existing 15%-85% guardrails. See `docs/52-file-manager.md`.
- **File manager text selection** — The file manager view no longer inherits `user-select: none` across the whole browser. Breadcrumbs, file names, status text, and preview content can now be selected with the mouse and participate in the global copy-on-select flow, while decorative mark/icon/age-bar columns remain non-selectable so copied list text stays clean. See `docs/52-file-manager.md`.
- **ACP harness on-demand memory** — `session/prompt` no longer carries the full per-lane memory body. Each prompt is now prefixed only with a short lane-context stub (~60–90 tokens) containing the active lane label, the lane roster, and a one-line nudge describing the `krypton-harness-memory` MCP tools (`memory_list`, `memory_get`, `memory_set`). Agents call those tools on demand instead of receiving the snapshot every turn. The leading resource block's URI changed from `krypton://acp-harness/memory.md` to `krypton://acp-harness/lane-context.md`, which also makes Codex `session/list` titles use the user's first text block instead of the literal memory URI for every session. Pi lanes still get the stub as a text block but cannot call MCP tools (no MCP host), which is acceptable. See `docs/98-acp-harness-memory-on-demand.md`.
- **ACP harness session list/resume** — `Cmd+P → 0` in the ACP Harness now opens a project-scoped session picker for the active lane's backend. The picker initializes only that backend, calls `session/list` with the harness project directory, and opens the selected historical session in a new lane using `session/resume` when advertised or `session/load` as a fallback. `session/load` replayed `user_message_chunk` and assistant/tool updates flow through the normal transcript renderer, and resume/load sessions receive the same capability-gated `.mcp.json` bridge plus `krypton-harness-memory` MCP descriptors as fresh lanes. See `docs/97-acp-harness-session-resume.md`.
- **ACP built-in memory auto-approval** — memory permission prompts now recognize Codex-style namespaced built-in tool ids such as `mcp__krypton_harness_memory__memory_set`, rendered permission labels such as `Tool: krypton-harness-memory/memory_set`, and uppercase titles such as `MEMORY_SET`, as well as the existing `/mcp/harness/` endpoint marker. Auto-approval still requires both a built-in memory marker and one of `memory_set`, `memory_get`, or `memory_list`, so same-named third-party or `.mcp.json` tools continue through the normal permission UI. See `docs/96-acp-built-in-memory-auto-approval.md`.
- **ACP harness scroll anchor stability** — Spec 95's body-preservation fix stopped full renders from resetting the active lane body, but non-bottom reading mode could still drift because `.acp-harness__lane-body` disables native browser scroll anchoring and streaming/pretext layout can change row heights above the viewport. `renderActiveTranscript()` now captures the first visible transcript row id plus its viewport offset before mutations and restores that same row afterward when `stickToBottom` is false. `schedulePretextLayout()` uses the same anchor around line measurement, and a `ResizeObserver` on the active body plus message rows reapplies the saved anchor for later async height changes. Bottom-stick lanes re-pin on those resize events. See `docs/95-acp-harness-scroll-stability.md`.
- **Pencil picker rename and local drawing leaders** — focused Pencil views now expose local leader `/` ("Open Drawing Here") and `?` ("New Drawing") through `PENCIL_LEADER_KEYS`, validated by `leader-keys.test.ts` alongside Markdown and ACP Harness bindings. `/` scans the current drawing's directory and replaces the current Pencil tab with the selected existing `.excalidraw` file; `?` prompts for a new file name in that directory, then routes through `openPencil(path)` so existing open tabs are refocused and missing files start as empty scenes that autosave on first change. Both Pencil pickers now support `r` on an existing row to rename via `rename_pencil_file` without overwriting an existing destination. The original global `Leader e` picker remains unchanged. See `docs/71-pencil-window.md`.
- **ACP harness scroll stability** — full `render()` paths used to clear `dashboardEl.innerHTML` and recreate the active lane's `.acp-harness__lane-body`, which reset `scrollTop` to 0 on a fresh node and lost any in-flight streaming layout. Numeric save-and-restore of `scrollTop` could not beat late layout passes (pretext line wrap, markdown image height, code highlight) that change `scrollHeight` after the restore. The body element's DOM identity is now preserved across `renderDashboard()`: it is detached before the `innerHTML = ''` clear, reused inside the rebuilt active lane shell when the lane id matches, and `renderActiveTranscript()` then performs its existing per-row diff. The browser keeps the real scroll position untouched and Spec 94 streaming wrappers also survive across full renders. `g`/`G` keys update `stickToBottom` consistently; `applyStickyScroll()` keeps `savedScrollTop` as a defensive fallback. See `docs/95-acp-harness-scroll-stability.md`.
- **ACP harness render batching and transcript render caches** — high-volume ACP event streams no longer force a synchronous full dashboard rebuild for every message/tool chunk. `onLaneEvent()` now routes expensive refreshes through a `requestAnimationFrame`-backed `scheduleRender()` so multiple chunks coalesce into one paint. Lightweight `available_commands` and `mode_update` events patch only the composer or lane head. Assistant rows cache parsed/highlighted markdown per transcript item, and pretext rows cache measured line layouts keyed by text, width, font, and line-height, so old visible rows do not repeat markdown parsing or line measurement on every render. Streaming-row diff path keeps the existing wrapper element and swaps only its children, so streaming markdown updates no longer churn the wrapper DOM each frame. An attempted `content-visibility: auto` on transcript rows was reverted: the `contain-intrinsic-size` placeholder undersized off-screen rows so `body.scrollHeight` was below true height and sticky auto-scroll settled above the real bottom. See `docs/94-acp-harness-render-performance.md`.
- **ACP harness per-line stagger reveal — REMOVED.** The fade-in stagger animation introduced in Spec 93 (`.acp-harness__pretext-line[data-anim="in"]` and the per-paragraph reveal on assistant markdown) was incompatible with the Spec 94 streaming `replaceChildren` path: every chunk rebuilt the markdown DOM, so block nodes had a shorter lifetime than the 180ms animation, and the per-lane index counters never matched the freshly-recreated nodes. The result was that no stagger animation was ever visible during streaming. The `acp-harness-line-in` keyframe, the `data-anim="in"` rules, the `applyAssistantBlockStagger()` helper, and the `assistantBlockCounts` / `pretextLineCounts` lane state were removed. Row entrance, caret blink, busy pulse, and streaming pulse animations are unaffected. See `docs/94-acp-harness-render-performance.md`.
- **ACP harness lane picker (no auto-spawn)** — opening the harness no longer fan-spawns six ACP subprocesses. The view boots empty and shows a hint to add a lane via the leader-key picker. Three new local leader keys ride under `Cmd+P`: `+` opens the backend picker overlay (lists installed adapters with running-lane counts; `j/k` move cursor, `Enter` spawns a fresh lane, `Esc` cancels), `_` closes and disposes the active lane, `=` toggles the metrics overlay. The previous `m` binding was silently broken (`m` is in `GLOBAL_LEADER_RESERVED_KEYS`); replaced with the unreserved `=`. Lane index is monotonic across the session via `nextLaneIndex` so display names like `Claude-1`, `Claude-2` never collide even after closes. Multiple lanes per backend are supported. See `docs/92-acp-lane-picker.md`.
- **ACP harness lane resource metrics** — every lane head now shows a CPU%/RSS chip computed from the adapter PID and its descendants (MCP servers reparented under the adapter via `setsid`). A long-lived `sysinfo::System` (`src-tauri/src/process_metrics.rs`) refreshes once per poll and the new `acp_get_lane_metrics` command returns aggregate + per-process data for every live lane in a single round trip; frontend polls every 2 s and patches only the affected `.acp-harness__lane-head` so the transcript is unaffected. CPU bucket drives chip color (cyan / amber / red); contextual leader key `Cmd+P → m` opens an overlay with the full process tree per lane. See `docs/91-acp-lane-resource-metrics.md`.
- **ACP harness plan tracking — pinned panel** — `session/update { sessionUpdate: 'plan' }` was previously appended to the lane transcript as an inline card, so the current plan scrolled away as soon as new tool calls arrived. Plan entries now live on `HarnessLane.plan` and render into a single floating `.acp-harness__plan` panel anchored top-right of the harness body (z-index below the memory/help overlays). Header shows `// plan` with a `done/total` progress count; rows render `[ ]/[~]/[x]` with status-driven colors (pending dim, in_progress amber, completed green strikethrough) and a 2px left-border priority accent. Panel auto-hides when no plan, clears on `#restart`/`#new`/`#new!`, and collapses with `p` in transcript focus. The legacy inline `appendTranscript('plan', ...)` call and the `'plan'` member of `HarnessTranscriptItem.kind` were removed — the panel is the single surface for plan state. See `docs/90-acp-plan-tracking.md`.
- **ACP diff preview, gated writes, and path scoping** — `tool_call.content[].diff` payloads (Codex/Claude/Gemini) now render as inline +/- hunks with line numbers via the shared `src/acp/diff-render.ts` module (extracted from the legacy `acp-view.ts` so both views share one renderer). `fs/write_text_file` requests are no longer applied immediately: the Rust handler reads the current disk content as `oldText`, parks the JSON-RPC reply on a `oneshot` channel, emits a `fs_write_pending` event, and waits for the user's accept/reject decision via the new `acp_fs_write_response` Tauri command before writing or replying with an error. Both fs handlers route through `validate_fs_path`, which canonicalizes the requested path against the lane's project root and rejects anything that escapes. Keys: `a` accept, `r` reject, `A` accept-all-this-turn, `R` reject-all-this-turn — reusing the existing `acceptAllForTurn`/`rejectAllForTurn` flags. See `docs/89-acp-diff-preview.md`.
- **ACP fs/* activity surface** — Krypton's Rust ACP backend already implemented `fs/read_text_file` / `fs/write_text_file` and advertised both in `clientCapabilities.fs`, but every read/write was handled silently. The harness now emits an `fs_activity` event on every inbound fs request (success and failure), and the frontend renders a `📖 read` / `✏️ wrote` / `✗ failed` chip inline in the lane transcript with the affected path. Visibility-only — no diff preview or write-permission gate yet (those land in a follow-up spec). Pi-1 stays N/A because pi-acp does not request the fs capability. See `docs/88-acp-fs-activity-surface.md`.
- **ACP extended session updates** — the Rust forwarder already accepted `available_commands_update` and `current_mode_update` notifications from agents, but the TS dispatcher silently dropped them. The frontend now consumes both: each lane stores its own slash-command catalog and current mode, the lane head paints a purple mode chip ("Claude-1 · plan"), and the composer pops a slash-command palette when the draft starts with `/` (↑↓ select · Enter/Tab insert · Esc dismiss). Pi-1 emits neither so both surfaces stay quiet for it. See `docs/87-acp-extended-session-updates.md`.
- **ACP Harness Droid-1 lane** — Factory's `droid` CLI ships first-party ACP support, so Krypton spawns it directly with `droid exec --output-format acp` instead of going through the third-party `droid-acp` npm wrapper. Droid-1 is a "regular" lane (full `.mcp.json` bridge, full per-lane memory MCP, permission rail engaged) — opposite archetype from Pi-1. Auth via `FACTORY_API_KEY` env (full login env injection covers it); device-code OAuth still needs a TTY, so users on that path run `droid` once outside Krypton. Optional `acp_harness.lane_models["droid"]` adds `-m <model-id>` at spawn (default model is `claude-opus-4-7`). See `docs/86-acp-droid-lane.md`.
- **Contextual leader keys** — content views can now expose focused-view leader actions through static local metadata plus runtime `getLeaderKeyBindings()`. Global and local leader keys share the existing one-key Compositor layer but are validated at development time so they never conflict: `src/leader-keys.ts` defines global reserved keys and `src/leader-keys.test.ts` fails on duplicate local/global metadata. The global `handleCompositorKey()` switch remains in place for v1. Markdown view adds `Leader ;` for link hint mode, hidden from which-key when no links are available. See `docs/85-contextual-leader-keys.md`.
- **ACP backend env injection** — ACP adapter subprocesses (Claude/Gemini/Codex/OpenCode/Pi) now inherit the full cached login env (`cached_login_env()`) instead of just `PATH`. Provider API keys exported in the user's login shell (`ZAI_API_KEY`, `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, etc.) reach the adapter, so e.g. Pi-1 can authenticate via env var without writing `~/.pi/agent/auth.json`. Removes the obsolete `cached_login_path()` helper from `acp.rs` (PTY's `cached_login_env()` already provides PATH).
- **ACP Harness Pi-1 lane** — `pi-acp` (https://github.com/svkozak/pi-acp) is registered as a harness backend, spawning the `pi` coding agent (`@mariozechner/pi-coding-agent`) over ACP JSON-RPC. Pi-1 deliberately skips the `.mcp.json` bridge and the per-lane `krypton-harness-memory` server because pi has no MCP host by design ("No MCP. Build CLI tools with READMEs, or build an extension that adds MCP support."), and pi has no permission gate either — file edits and bash run immediately. The lane chip surfaces a `⚠ unsandboxed` marker so the safety delta versus regular lanes is visible. Built-in `inferLaneModelName` already reads the agent-reported model generically; no `session/set_model` is sent for pi. Prerequisites: `npm install -g @mariozechner/pi-coding-agent pi-acp`, configure provider via env var or `pi /login`. See `docs/84-acp-pi-lane.md`.
- **ACP Harness shared `.mcp.json`** — Codex-1, Gemini-1, and OpenCode-1 lanes now load servers from the project's `.mcp.json` (Anthropic project-scope MCP config) so a single config gives every lane the same tools. Frontend reads `<projectDir>/.mcp.json`, expands `${VAR}` / `${VAR:-default}` against the cached login env, translates Claude's object-shaped `env`/`headers` into ACP's array form, and capability-gates `http`/`sse` against `agentCapabilities.mcpCapabilities` from `initialize`. Claude-1 is skipped because the Claude Code adapter loads `.mcp.json` natively. Rust splits `acp_initialize` into `initialize` + `set_mcp_servers` + `session_new` so the bridge has a place to inject between capability discovery and `session/new`. The `krypton-harness-memory` server stays appended after the bridged list. See `docs/83-acp-shared-mcp-config.md`.
- **Hint file paths open in Helix** — selecting the built-in `filepath` hint now opens a terminal tab running `hx <path>` directly instead of copying the path to the clipboard. Quick File Search and hint mode share the same editor opener, so relative paths inherit the focused pane or content-view cwd and the tab closes automatically when Helix exits instead of returning to a shell. The backend now emits `pty-exit` from a direct child-process wait as well as PTY reader EOF/error, making direct editor tab cleanup reliable. URL/email/custom hints keep their configured `Open`/`Copy`/`Paste` behavior. See `docs/12-hint-mode.md` and `docs/82-global-hint-mode.md`.
- **Global hint mode for DOM views** — `Leader Shift+H` now works on every focused pane: terminal panes use the existing xterm buffer scanner, content views (markdown, hurl, vault, diff, pencil, file manager, ACP harness, agent) use a new DOM scanner that walks visible text nodes, runs the configured `[hints]` regex rules, and overlays labels via `Range.getClientRects()` + `position: fixed`. `Open` and `Copy` actions behave identically; `Paste` falls back to `Copy` in DOM mode (no PTY target). The `Cmd+Shift+H` global accelerator stays terminal-only to avoid colliding with text inputs in DOM views. See `docs/82-global-hint-mode.md`.
- **Global copy-on-select** — any text selection in a DOM view (markdown viewer, hurl client, diff view, vault view, ACP harness, agent view, dashboards, etc.) now auto-copies to the clipboard on `mouseup` or navigation `keyup`, matching the existing terminal-pane behavior. Selections inside `<input>`, `<textarea>`, or `[contenteditable]` are skipped to preserve editing semantics; xterm canvases are unaffected because their selection is not part of the DOM Selection API. See `docs/81-global-copy-on-select.md`.
- **ACP Harness memory drawer key capture** — `Ctrl+N` / `Ctrl+P` are now handled by the open memory drawer before global lane-switch routing, so they move the selected memory row instead of leaking to the main harness view. The cursor helper also normalizes the key value for stable `n/p` handling. See `docs/72-acp-harness-view.md`.
- **ACP Harness permission confirmation liveness** — accepting or rejecting a prompted non-memory ACP permission now immediately appends the resolution row and returns the lane composer to busy state before awaiting the permission-response IPC. If the reply fails, the same permission is restored for retry. This prevents a slow adapter reply from leaving the permission banner looking stuck after confirmation. See `docs/72-acp-harness-view.md`.
- **ACP Harness configurable lane models** — `acp_harness.lane_models.<backend>` in `krypton.toml` declares an `active` model id (and optional `models` allow-list) per harness backend. OpenCode applies it after `session/new` via `session/set_config_option` (with `session/set_model` fallback); other supported lanes use their documented spawn or generic ACP model paths. Empty/missing keeps the existing adapter default. The lane model chip prefers the configured active value over agent-reported metadata. See `docs/06-configuration.md` and `docs/72-acp-harness-view.md`.
- **ACP Harness image transcript indicator** — submitted user transcript rows now show a compact image-count attachment chip when the turn included pasted, dropped, or screen-captured images. The prompt still sends full ACP image blocks with base64 data and `file://` URIs; transcript history stores only the count so long sessions do not retain thumbnail payloads. See `docs/72-acp-harness-view.md`.
- **ACP Harness lane model chip** — lane headers now show a compact model chip only when Krypton can determine the model from ACP metadata or from an explicitly selected backend model such as OpenCode's `zai-coding-plan/glm-5.1`; unknown models are hidden. See `docs/72-acp-harness-view.md`.
- **ACP Harness busy composer liveness** — while the active lane is running a prompt, the composer chip now shows `<lane> running · m:ss · Ctrl+C cancel`, updates once per second, and uses a subtle lane-accent pulse so long silent agent turns do not look frozen. The timer clears on stop, error, or lane restart. See `docs/72-acp-harness-view.md`.
- **ACP Harness tool output glow text** — tool output excerpts now render as plain monospace text with label-specific text colors and text-shadow glow instead of syntax-highlighted markup or background glow panels. Assistant markdown code highlighting is unchanged. See `docs/72-acp-harness-view.md`.
- **ACP Harness memory drawer selection** — memory rows in the harness drawer can now be selected by click, `Ctrl+N`/`Ctrl+P`, arrow keys, page keys, and Home/End. While the drawer is open, the composer input line is hidden and ordinary typing is captured so drafts do not change underneath the memory view. The drawer docs now match the implemented `Ctrl+M` / `Cmd+M` toggle. See `docs/72-acp-harness-view.md`.
- **ACP Harness composer cwd + branch and image file URIs** — the harness composer status line now shows the project working directory plus the current Git branch when the project directory is inside a repository. Detached HEAD displays as `HEAD <sha>` and non-Git directories show only the cwd. Staged pasted/dropped/captured images now render as removable placeholder chips and send ACP image blocks with base64 data plus a local `file://` URI to the saved image path. See `docs/72-acp-harness-view.md`.
- **ACP Harness screen capture staging** — global `Ctrl+Shift+S` now routes captured PNGs into the focused ACP Harness instead of the smart prompt dialog when the harness view is active. The capture is staged in the active lane composer through the same image path as paste/drop and is sent as an embedded ACP image content block on the next prompt. Non-harness focus keeps the previous prompt-dialog queue behavior. See `docs/63-screen-capture-prompt.md` and `docs/72-acp-harness-view.md`.
- **ACP Harness Zen Mode** — toggleable focused layout (`Cmd+.`) collapses inactive lanes into a thin left status rail (status dot + lane name only) while the active lane keeps its full transcript on the right. The harness title hides; cwd and global counts stay. Per-project preference persists via `localStorage`. Inactive lanes that need permission or have errored pulse their rail dot via a single keyframe — no extra DOM, no layered effects. Composer, tool rows, memory peek, and active lane body are unchanged. See `docs/80-acp-harness-zen-mode.md`.
- **ACP Harness memory MCP auto-allow** — built-in `krypton-harness-memory` MCP permission prompts for `memory_set`, `memory_get`, and `memory_list` are now auto-allowed by default and logged in the lane transcript, keeping the memory board agent-managed while leaving non-memory tool permissions on the explicit composer flow. See `docs/72-acp-harness-view.md` and `docs/73-acp-harness-mcp-memory.md`.
- **ACP Harness fresh-session commands** — harness hash commands now include `#new` for a fresh active-lane ACP session, `#new!` for fresh session plus active-lane memory clear, and `#mem clear` for clearing only the active lane's persisted memory document. Fresh sessions await old-client disposal and use a lane spawn epoch plus disposed-event guard to ignore late updates from replaced subprocesses. See `docs/79-acp-harness-fresh-session-commands.md`.
- **Vault view perf** — cold-open path drops from 346 IPC roundtrips to 3 by replacing the per-file `cat` loop with a single bulk `read_vault_files` Rust command (parallel `tokio::spawn_blocking` + lossy UTF-8 decode), pre-building a suffix-aware `slugMap` for O(1) wikilink resolution, and deferring heading parsing from index time to `openFile()` (gated by a `headingsLoaded` flag). A static `indexCache` keyed by `vaultRoot` skips re-indexing on reopen; explicit `r` reload always rebuilds and overwrites. See `docs/78-vault-view-perf.md`.
- **ACP Harness view (Leader Y)** — multi-lane ACP orchestration tab that starts empty, lets the user add installed backends through `Cmd+P → +`, routes prompts to one active lane, displays a lane dashboard plus command center, handles per-lane permissions, and keeps project-scoped MCP lane memory visible in a read-only board. Built-in OpenCode lanes select `zai-coding-plan/glm-5.1` through ACP session configuration so Krypton owns the default lane model. See `docs/72-acp-harness-view.md`.
- **Pencil window (Leader e)** — embed `@excalidraw/excalidraw` as a new content view to open and edit `.excalidraw` files in-app. React + Excalidraw are lazy-loaded so the main bundle is unaffected. Picker scans `[pencil] dir` recursively (mtime sorted) with "+ New drawing" first row; opening the same file twice refocuses the existing tab. Autosave is debounced (800 ms) via `serializeAsJSON`; `Cmd+S` flushes immediately; atomic temp+rename in Rust. Theme follows Krypton bg luminance. See `docs/71-pencil-window.md`.
- **ACP agent windows (Leader A)** — second, parallel agent window backed by the [Agent Client Protocol](https://agentclientprotocol.com). Spawns built-in external adapters (Claude Code, Gemini CLI, Codex, OpenCode) over newline-delimited JSON-RPC and surfaces tool calls, plans, thoughts, and inline permission prompts. Lives in `src/acp/` + `src-tauri/src/acp.rs`; the existing pi-agent at `src/agent/` is untouched. See `docs/69-acp-agent-support.md`.
- **Quick file search (Cmd+O)** — global modal backed by `fff-search` with long-lived per-project pickers (LRU 8), persistent LMDB frecency, and `.git`-aware root resolution. Enter copies the relative path to the clipboard, Cmd+Enter copies the absolute path — never auto-pasted, so behavior is uniform across terminal/agent/hurl/markdown windows. **Tab toggles into grep mode** (content search reusing the same picker; copies `path:line:col`). See `docs/68-quick-file-search.md`.
- **Matrix glyph atlas** — per-frame `fillText` replaced with `drawImage` blits from a pre-rasterized `OffscreenCanvas`. Eliminates CoreText/GPU-process IPC from the hot path; matrix runs at 60 fps with single-digit CPU. See `docs/67-matrix-glyph-atlas.md`, `docs/64-matrix-animation-cpu-burn.md` (Phase 2).
- **Hurl client window** — keyboard-driven `.hurl` runner with tree sidebar, streaming output, per-file cache, and persistent sidebar state. See `docs/65-hurl-client-window.md`.

## M0 — Scaffold (Week 1-2)

- [x] Initialize Tauri v2 project with TypeScript frontend
- [x] Install xterm.js and addons as frontend dependencies
- [x] Configure Tauri window: fullscreen, borderless, transparent
- [x] Set up frontend build pipeline (Vite + TypeScript, transparent HTML/CSS shell)
- [x] Verify transparent window renders correctly on macOS

## M1 — Single Session (Week 3-4)

- [x] Implement PTY spawn via `portable-pty` in Rust backend
- [x] Create Tauri commands: `spawn_pty`, `write_to_pty`, `resize_pty`
- [x] Wire xterm.js `onData` -> IPC -> PTY write path
- [x] Wire PTY read -> Tauri event -> `xterm.write()` path
- [x] Render a single terminal window on the transparent workspace
- [x] Verify interactive shell works (typing, output, ctrl+c)

## M2 — xterm.js Integration (Week 5-7)

- [x] Enable `@xterm/addon-webgl` with canvas fallback
- [x] Integrate `@xterm/addon-fit` for automatic resize
- [x] Validate SGR rendering (colors, bold, italic, etc.)
- [x] Test alternate screen buffer (vim, htop, less)
- [x] Test truecolor output

## M3 — Compositor & Windows (Week 8-11)

- [x] Build compositor layer: workspace as transparent fullscreen virtual desktop
- [x] Window DOM structure: cyberpunk chrome with titlebar (dynamic shell title via OSC + CWD status + status dot), content area (xterm.js body + sidebar decoration), bottom bar
- [x] Custom window chrome rendering — sci-fi style with glowing cyan borders, telemetry sidebar, bottom bar decorations
- [x] Grid layout engine: resolve `{ col, row, col_span, row_span }` to screen coordinates
- [ ] Support absolute position overrides
- [x] Keyboard-driven window focus: directional (H/J/K/L) and by index
- [x] Window creation/close via keyboard
- [x] Focus indicator — cyan border glow + box-shadow on focused window, dimmed borders on unfocused
- [x] Ship built-in workspace presets — Focus Layout: focused window left (full height), remaining stacked right; toggle via `Leader f`
- [x] Pin windows — `Leader p` toggles pin on focused window; pinned windows stick to right column in Focus layout, skipped during focus cycling; visual indicator (diamond icon) in title bar; pin/unpin sound effects; CSS class `krypton-window--pinned`
- [ ] Responsive recalculation on screen resolution change — basic version done, needs testing
- [x] Webview content type — in-app browser panes via Tauri child webviews (see `docs/102-webview-windows.md`)

## M4 — Keyboard System & Workspaces (Week 12-15)

- [x] Implement Input Router with mode system (Normal, Compositor, Resize, Move, Swap, CommandPalette)
- [x] Leader key activation and single-action compositor mode
- [x] Resize mode: arrow keys resize focused window, step size configurable
- [x] Move mode: arrow keys reposition focused window
- [x] Window swap via keyboard (Leader+s then direction h/j/k/l)
- [x] Window maximize/restore via keyboard (Leader+z toggle)
- [x] Command palette: fuzzy-searchable overlay (`Cmd+Shift+P`) listing all actions with keybindings, fuzzy subsequence matching with score-based ranking, category tags, highlighted match characters. `src/command-palette.ts` module with action registry for Window/Tab/Pane/Layout/Mode/Terminal categories (~35 actions)
- [ ] Workspace switching via hotkeys (`CmdOrCtrl+1/2/3`, next/prev)
- [x] Animation engine: morph (bounds transition), slide (horizontal), crossfade (opacity), window entrance/exit effects
- [x] Configurable animation style, duration, easing (AnimationConfig with AnimationStyle, AnimationEasing, WindowEffect enums)
- [x] Keyboard input buffering during transitions (buffered in Normal mode, replayed after animation)
- [ ] Session pool: preserve PTY sessions across workspace switches
- [x] Mode indicator UI
- [x] Quick Terminal: persistent overlay terminal (Cmd+I toggle), centered on screen, own PTY, animated show/hide

## M5 — Tabs & Panes (Week 16-18)

- [x] Tab bar UI within each window (keyboard-navigable) — `.krypton-window__tabbar` with tab elements, active indicator, auto-show when >1 tab (configurable via `always_show_tabbar`)
- [x] Create/close/switch tabs via keyboard — `Leader t` (create), `Leader w` (close), `Leader [/]` (prev/next)
- [x] Move tab to another window via keybinding (`Leader T` then window index) — enters TabMove mode, press 1-9 to select target window
- [x] Horizontal/vertical pane splits within a window — `Leader \` (vertical), `Leader -` (horizontal); binary tree of splits with configurable default direction
- [x] Keyboard navigation between panes — `Leader Alt+h/j/k/l` (directional), `Leader Alt+x` (close pane); spatial nearest-neighbor search
- [x] Session manager: track multiple PTY instances per window — `sessionMap: Map<SessionId, {windowId, tabId, paneId}>` for O(1) PTY output routing; each pane owns its own PTY session

## M6 — Config, Theming & Custom Themes (Week 19-21)

- [x] TOML config parser with `serde` — loads `~/.config/krypton/krypton.toml`, creates default on first run, merges with defaults for missing fields
- [x] Config applied to frontend: shell program/args, font (family/size/line_height), terminal (scrollback/cursor_style/cursor_blink), theme color overrides, Quick Terminal sizing, workspace gap/step sizes
- [x] Theme engine: load built-in themes + custom `.toml` files from themes directory — `src-tauri/src/theme.rs` embeds built-in themes via `include_str!`, resolves by `theme.name`, supports custom themes from `~/.config/krypton/themes/*.toml`, applies `[theme.colors]` overrides on top
- [x] Theme scope: terminal colors, window chrome, workspace background, UI elements — full theme TOML structure (meta, colors, chrome, focused, workspace, ui) parsed and sent to frontend as `FullTheme`
- [x] Apply theme as CSS custom properties (instant update across all windows) — `FrontendThemeEngine` sets 50+ `--krypton-*` CSS custom properties on `document.documentElement`; `styles.css` uses `var()` throughout; existing xterm.js terminals updated via `terminal.options.theme` on change
- [x] Hot-reload via `notify` crate (config + theme files) — filesystem watcher on `~/.config/krypton/` with 300ms debounce; emits `theme-changed` and `config-changed` Tauri events; frontend listens and applies instantly
- [x] Per-window accent colors — 10-color cyberpunk palette (cyan, magenta, amber, green, violet, orange, pink, teal, gold, red); each window gets a unique color for chrome, borders, corners, tabs; colors recycled on window close
- [ ] Ship built-in terminal themes (dark, light, solarized, legacy-radiance) — krypton-dark and legacy-radiance TOML files created and loaded by engine; krypton-light and solarized not yet created
- [ ] Ship 3 built-in chrome styles (macos, minimal, none)
- [ ] Full keybinding customization with conflict detection
- [ ] Command palette theme switching

## M7 — Sound Effects (Week 22-23)

- [x] Sound engine: Rust backend (`src-tauri/src/sound.rs`) via `rodio` crate with dedicated audio thread. Replaced frontend Web Audio API for reliability. See `docs/17-sound-themes.md`
- [x] WAV-based sound packs (deep-glyph, mach-line) bundled as Tauri resources under `src-tauri/sounds/`
- [x] Frontend thin IPC wrapper (`src/sound.ts`): `play()` / `playKeypress()` call Tauri commands
- [x] 5 Tauri commands: `sound_play`, `sound_play_keypress`, `sound_apply_config`, `sound_load_pack`, `sound_get_packs`
- [x] Overlap management in Rust: MAX_CONCURRENT=8, keypress throttle 25ms, event cooldown 50ms
- [x] Integration: compositor + input-router call `SoundEngine.play()` at each action point (48 call sites unchanged)
- [x] Configuration: `[sound]` TOML section applied via `applyConfig()` — hot-reload updates Rust engine directly
- [x] Keypress sounds: per-key routing (Backspace/Enter/Space/Letter), configurable `keyboard_volume`
- [x] Sound theme switching via command palette
- [ ] Custom sound pack loading from `~/.config/krypton/sounds/`
- [x] Graceful degradation when no audio device is available

## M8 — Polish (Week 24-27)

- [x] Vim-like Selection mode (`src/selection.ts`) — keyboard-driven text selection with virtual cursor, h/j/k/l/w/b/e/0/$/gg/G navigation, v (char-wise) and V (line-wise) visual selection, y to yank to clipboard. Enter via `Leader v` or `Leader V`
- [x] Hint mode (`src/hints.ts`) — Rio-style pattern matching: `Cmd+Shift+H` or `Leader Shift+H` scans visible buffer for regex patterns (URLs, file paths, emails), overlays keyboard labels on matches, type label to act (Open/Copy/Paste). Configurable alphabet, rules, and per-rule actions via `[hints]` TOML config.
- [x] Terminal shader effects (`src/shaders.ts`) — CSS/SVG filter-based post-processing: 5 presets (crt, hologram, glitch, bloom, matrix) with scanlines, chromatic aberration, displacement, bloom glow. Per-pane ShaderInstance with overlay div + animated keyframes. Configurable via `[shader]` TOML section; `Leader g` cycles preset, `Leader G` toggles globally.
- [x] Progress bar via ConEmu `OSC 9;4` — Rust backend inline parser detects progress sequences in PTY output, emits `pty-progress` Tauri event. Frontend renders a large translucent SVG arc gauge centered in the window's content area behind terminal text (fills clockwise for normal progress, orbits for indeterminate, red for error, amber for paused) with percentage text, status labels, and a subtle titlebar scanline sweep. Per-window accent color aware. Used by Zig CLI, systemd, Amp, etc.
- [x] 3D perspective depth — CSS `perspective` on content containers with `translateZ` layering: terminal text at back (0), progress gauge mid (10px), shader overlay (20px), selection cursor/dividers at front (30px). Configurable via `[visual] perspective_depth` (default 800px, 0 = disabled). GPU-composited, hot-reloadable.
- [x] Context-aware extensions — process detection via `tcgetpgrp()` on PTY master fd (500ms poller), `ExtensionManager` activates built-in extensions when matching foreground processes are detected. First extension: **Java Resource Monitor** — top bar shows `[JAVA] MainClass PID`, bottom bar shows live `HEAP`, `GC`, `CPU%`, `RSS` stats via `jstat -gc` + `ps` (2s poll). Bars are real layout elements (not overlays), terminal resizes via `addon-fit`. Configurable via `[extensions] enabled/poll_interval_ms`.
- [x] Overlay dashboard infrastructure — `DashboardManager` framework (`src/dashboard.ts`) for registering and displaying full-screen overlay panels. Generic lifecycle (`onOpen`/`onClose`/`onKeyDown`), `Mode.Dashboard` input routing, CSS animated show/hide (z-index 9500), command palette integration. First dashboard: **Git Status** (`Cmd+Shift+G`) — shows current branch, staged/modified/untracked/deleted file counts, and changed file list. Uses new `run_command` Tauri command for non-PTY subprocess execution.
- [x] OpenCode Dashboard (`Cmd+Shift+O`) — reads OpenCode's local SQLite database (`~/.local/share/opencode/opencode.db`) via new `query_sqlite` Tauri command (`rusqlite`, read-only). Displays: aggregate stats (sessions, messages, output tokens, cache reads, cost), 20 most recent sessions with message counts/tokens/diffs/duration, model usage breakdown (model, provider, count, output tokens), and top 15 tool usage with bar chart. New `query_sqlite` command: generic read-only SQLite query executor with write-statement rejection, 1000-row limit, 5-second busy timeout.
- [ ] `@xterm/addon-search` integration with keyboard-driven search overlay
- [ ] IME support testing and fixes
- [ ] Performance profiling (latency, animation FPS, transparent rendering overhead)
- [x] Fix macOS transparency freeze — removed `backdrop-filter: blur()` from all elements (`.krypton-window`, Quick Terminal, which-key, command palette, dashboard, hint-toast). On macOS, `backdrop-filter` in a transparent WKWebView causes the compositor to snapshot/freeze the desktop behind terminal windows when focused. Also added `.xterm-scrollable-element` CSS override to fix xterm.js setting opaque inline `backgroundColor`. See platform gotcha in `docs/04-architecture.md`.
- [x] Sound engine moved to Rust backend — replaced frontend Web Audio API with `rodio` crate on dedicated audio thread. WAV packs bundled as Tauri resources under `src-tauri/sounds/`. Frontend is thin IPC wrapper. See `docs/17-sound-themes.md`.
- [x] SSH session multiplexing — detect active SSH connections in terminal panes via process tree inspection (`SshManager` in `src-tauri/src/ssh.rs`), clone sessions using OpenSSH `ControlMaster` auto-multiplexing. **Remote CWD inheritance**: cloned sessions start in the same directory via invisible PTY probe (`stty -echo` + OSC 7337 private escape sequence); passive OSC 7 hostname filtering as fallback (uses `hostname` crate to distinguish local vs remote CWD). Keybindings: `Leader c` (clone to new tab), `Leader Shift+C` (clone to new window). Command palette actions. Titlebar shows `SSH: user@host`. Config: `[ssh]` section with `enabled`, `control_persist`, `clone_target`. Socket dir: `~/.config/krypton/ssh-sockets/`. See `docs/28-ssh-session-multiplexing.md`.
- [x] Notification overlay (`src/notification.ts`) — `NotificationController` with glitch-decode text animation, per-level color coding (info/success/warning/error/system), auto-dismiss with timer bar, max 6 stacked. OSC detection (OSC 9, 777, 99/kitty) via `terminal.parser.registerOscHandler()`. Container mounted on `document.body` with `position: fixed`, repositioned via `alignTo()` to anchor to focused window bounds. Programmatic API for any frontend subsystem. See `docs/40-notification-overlay.md`.
- [x] Cursor trail — rainbow flame particle effect (`src/cursor-trail.ts`) on both mouse cursor (document-level `mousemove` capture) and terminal text cursor (polls `buffer.active.cursorX/Y` via compositor). Particles drift upward with turbulence, cycle rainbow hues, fade with quadratic falloff. Teardrop-shaped with radial gradient + glow. Appended to `document.body` (z-index 99999). Togglable via `toggle()`.
- [x] Agent markdown visual effects — futuristic CSS effects on AI response rendering: scanline overlay + materialize animation on assistant messages, neon glow pulse on headings, animated edge-sweep on code blocks, holographic shimmer on blockquotes, traveling pulse on horizontal rules, hover-highlight on table rows, custom `▸` bullets, neon glow on inline code/links/bold. All pure CSS, GPU-composited, theme-aware via `--krypton-window-accent-rgb`. See agent markdown effects section in `docs/42-pi-agent-integration.md`.
- [x] Smart Prompt Dialog (`src/prompt-dialog.ts`) — global modal (`Cmd+Shift+K`) that composes a prompt and dispatches it to an active `claude` terminal tab via `write_to_pty`. Auto-detects Claude sessions through the `process-changed` event + `processBySession` cache on Compositor. Persistent target chip shows `→ Claude <cwd> pid <N>` with `Cmd+,` to reopen the picker (arrow/1-9/Enter/Esc) when multiple sessions exist. Supports `@path` autocomplete via existing `search_files` command (fuzzy-matched, cached per CWD with 10s TTL, positioned via mirrored-div caret helper in `src/caret-position.ts`), `@selection` inline-expanded from the focused xterm selection. Last-used target remembered across opens within the session. Target window gets a 600ms accent-glow flash on dispatch. New `Mode.PromptDialog`, magenta-accented dialog chrome to visually distinguish from the cyan command palette. See `docs/61-smart-prompt-dialog.md`.
- [x] Agent image attachment — paste (Cmd+V) or drag-drop images into agent windows. Up to 4 images staged as thumbnails above the input line, sent as multi-part `UserMessage` to vision-capable models. Vision gated by `vision = true` in TOML model preset. Non-vision models warned at submit time. Images stripped from JSONL session persistence (placeholder `[N images attached]` saved instead). See `docs/62-agent-image-attachment.md`.
- [x] Matrix animation CPU burn fix — OffscreenCanvas `fillText` on macOS WebKit has no glyph cache and IPCs per draw, so matrix/brainwave renderers were burning ~50–60% CPU per window when Claude was processing. Two fixes: (1) per-renderer frame cap in `animation-worker.ts` (matrix/brainwave/circuit-trace at 30 fps, flame at 60) — ~2× CPU reduction for the expensive renderers; (2) idle-timeout safety net in `claude-hooks.ts` that auto-stops the animation 60 s after the last hook event, preventing orphaned animations when `Stop`/`SessionEnd` hooks are dropped (crash, Ctrl+C, HTTP failure). See `docs/64-matrix-animation-cpu-burn.md`.
- [x] Agent `@path` fuzzy file search — typing `@` in the agent window's prompt input opens a fuzzy-ranked file picker scoped to the active `projectDir`. Uses the existing `search_files` Tauri command (respects `.gitignore`, caps 50k entries) cached per directory with 10 s TTL and shared with the Smart Prompt Dialog. `@` must be at start-of-line or preceded by whitespace. Tab/Enter accept, Esc dismisses, arrow keys navigate the dropdown. Takes precedence over the slash-command autocomplete; reuses the amber-phosphor dropdown aesthetic. Implemented in `src/agent/agent-view.ts` (`updateMentionState`, `rankMentionFiles`, `renderMentionPopup`, `acceptMention`) with matching CSS in `src/styles/agent.css`.
- [x] ACP Harness view — `Leader Y` / command palette opens a multi-lane ACP content tab for the focused CWD. The harness owns independent ACP clients, per-lane drafts and permissions, active-lane-only prompt dispatch, and dashboard transcript rendering. See `docs/72-acp-harness-view.md`.
- [x] ACP Harness MCP memory — the harness creates a tab-local memory store on the existing localhost hook server, passes lane-scoped HTTP MCP descriptors through ACP `session/new.mcpServers`, injects latest summaries, and renders a read-only observer memory board while agents manage create/update/delete/search/get themselves. See `docs/73-acp-harness-mcp-memory.md`.
- [x] ACP Harness memory persistence — lane memory is saved to per-project-dir JSON files under `~/.config/krypton/acp-harness-memory/` and restored on next harness creation for the same directory. Debounced atomic writes on change. See `docs/76-acp-harness-memory-persistence.md`.
- [x] ACP Harness fresh-session commands — `#new` starts a fresh active-lane ACP session while preserving memory, `#new!` also clears that lane's persisted memory, and `#mem clear` clears active lane memory without replacing the session. See `docs/79-acp-harness-fresh-session-commands.md`.
- [x] ACP Harness review quality matrix — a session-only history of `#review` rounds per authoring lane. After synthesizing a review, the convening lane self-reports via the default-on `review_outcome` MCP tool `{ blockers, warnings, reviewer_count, subject_label, findings? }`; the view records it in an in-memory `ReviewQualityStore`. Optional `findings[]` detail is capped at 500 items, strictly validated, and shown only in keyboard-expandable matrix rows. An observation, not a score (ADR-0004): raw counts plus evidence detail, no grade/verdict/diff-size/ranking/persistence. Surfaced like attention triage — a neutral `N reviews` footer depth indicator (ViewBus `review:quality`) plus a summon-on-demand read-only overlay (`Leader '`). See `docs/146-review-quality-matrix.md`.
- [x] Agent view persistent permission mode — a per-lane mode cycled with `Shift+Tab` that survives turn boundaries (NOT reset on `agent_start`): `normal` (every action prompts), `acceptEdits` (file writes auto-accepted, commands still prompt), `bypass` (everything auto-accepted incl. high-risk — the explicit no-safety state, loud pulsing red `⚠ BYPASS` status indicator). Decision logic is a pure, unit-tested module (`src/agent/permission-mode.ts`) layered on top of the spec-140 classifier (which is untouched). Precedence: per-turn reject-all > persistent mode > per-turn accept-all. `Esc` clears the mode and all per-turn flags. See `docs/147-persistent-permission-mode.md`.
- [x] Subscription credit usage view — `Leader $` opens a content-view window (`PaneContentType 'usage'`, `src/usage-view.ts`) with per-provider quota widgets (flat bordered blocks on a responsive grid, provider logos shared with the spec-125 lane rail): Claude, Codex, Copilot (`copilot_internal/user` quota snapshots: premium/chat/completions + monthly reset), and Cursor (real billing-cycle plan usage via the dashboard RPC `GetCurrentPeriodUsage` with the Keychain CLI JWT, spec 152 — `% of included usage` gauge with cycle-reset countdown plus a `$used · $included + $bonus` spend note; legacy request gauge and "not exposed" note retained as fallbacks). Claude (5h / weekly / weekly-Opus·Sonnet utilization + extra credits, from the OAuth usage endpoint via `src-tauri/src/usage.rs`, token read-only from `~/.claude/.credentials.json` and/or macOS Keychain — freshest expiry wins, since a stale file can linger while Claude Code keeps the live token in the Keychain; 180 s Rust-side cache) and Codex (5h primary / weekly secondary `rate_limits` from the newest `token_count` event in `~/.codex/sessions` rollout JSONL, shown "as of" last activity). Live reset countdowns, warn ≥ 80% / critical ≥ 95% bar colors, stale fallback to last good payload on fetch errors. No token refresh, no persistence, no new config. See `docs/151-subscription-usage-view.md`.
- [x] Live working diff — the Diff Window auto-refreshes at lane quiet points: an ACP harness publishes `harness:lane-idle { cwd }` on the ViewBus whenever a lane transitions to `idle`, and any diff window over the same repo (matched by cached `git rev-parse --show-toplevel`) re-collects and re-renders in place, preserving the current file + scroll position. Manual `r` refreshes mid-turn; hidden tabs defer to a dirty flag and refresh once on reveal (new `ContentView.onShow` hook). Collection moved to a single Rust command `collect_working_diff { cwd, staged }` (`src-tauri/src/git.rs`) that appends untracked files as synthesized additions (binary / >1 MiB skipped, listed by name) — replaces the old N+2 `run_command` calls and shares git primitives with the `#review` state collection. Static `synced HH:MM` nav indicator, never blinks. Deliberately NOT a filesystem watcher (ADR-0008). See `docs/155-live-working-diff.md`.
- [x] ACP Harness lane activity ticker — the busy composer chip shows a live activity segment (in-flight tool call title / `thinking…` / `writing…`) plus a running output-token counter next to the elapsed timer, e.g. `Claude-1 running · 8:23 · ⚒ Edit src/foo.ts · 12.4k tok`. Derived entirely from events the harness already receives (`tool_call`, `thought_chunk`, `message_chunk`, `usage`); setters are field-writes only and the existing 1 s composer tick paints — no new timers, CSS, or DOM. Cleared on stop/error/cancel. No sound (explicitly rejected). See `docs/156-lane-activity-ticker.md`.
- [x] ACP Harness concise mode — `Cmd+Shift+.` (sibling of Zen's `Cmd+.`) toggles a view-wide reading mode: tool cards collapse to their existing single head line (glyph + kind + subject + result + live timer; output sections, diff previews, artifact redactions hidden) and side-channel rows (`thought`, `fs_activity`, `memory`) hide entirely, while user/assistant rows and all interactive rows (`permission`, `fs_write_review`) stay fully visible. CSS-only via `.acp-harness--concise` root class — rows stay in the DOM so the spec-117 streaming parser keeps writing underneath and toggling back reveals everything instantly. Persisted per project in localStorage (zen pattern); a `concise` token in the composer meta row cues the state across reopens. See `docs/157-harness-concise-mode.md`.
- [x] ACP Harness blocked-lane visibility — lanes paused for a permission now announce **action required** and use a pulsing gold-tinted collapsed-row and Zen-rail state, with a persistent reduced-motion fallback. Permission decisions remain lane-local and never auto-switch the active lane. See `docs/72-acp-harness-view.md`.
- [x] Diff review comments — the Diff Window can attach review comments to a hunk/selection (`c`) and send a batch to a working ACP lane (`Shift+C` overlay → `s`) as a system turn, closing the "review → tell the agent what to fix" loop without leaving the diff. Comments carry a precise `file:line` + quoted-code anchor read from the diff2html DOM (both side-by-side and line-by-line renderers), batch GitHub-style, and route to the globally-unique target lane through the `HarnessDirectory` (no ViewBus broadcast — exactly one harness handles a send). Delivery reuses the spec-149 drain-on-idle pattern via a sibling `DiffReviewQueue`; the prompt is a JSON payload framed as untrusted data. Sent comments are marked + kept (never silently dropped on a closed/`#new`'d lane). Frontend-only, no Rust. Revised after Codex-1 review (4 blockers). See `docs/158-diff-review-comments.md`.
  - **Per-hunk anchoring fix + focus-hunk indicator** — `currentHunkAnchor`/`navigateHunk` were splitting on `.d2h-diff-tbody` (diff2html emits ONE per file/side), so a no-selection `c` always anchored the *whole file* and `n`/`N` couldn't step hunks within a file; both now use `splitHunks()` (partition on `.d2h-info`) via a shared `focusHunkInfo()`. New **focus-hunk indicator**: the viewport-top hunk (what `c` anchors to / `n`·`N` step from) is marked across its whole gutter (line-number accent, visible wherever you are in the hunk — the `@@` header also tints + gets a `← here` chip) (A), with a `hunk L<a>–<b>` nav readout (B). Driven off a per-render `focusCache` (hunk bottoms pre-measured once) so scrolling only does arithmetic + touches the DOM when the focus hunk changes — no per-frame `splitHunks`/layout (rAF-throttled, listener + rAF cleaned up on dispose). Frontend-only.
- [x] Diff review priority — the authoring lane self-reports a reading-order hint per change (default-on MCP tool `mark_review_priority { ranges }`, new-side line anchors, `high`/`routine` only — `normal` is the unreported default, optional short `reason` per range), and the Diff Window pre-triages: `routine` hunks collapse in place to a one-line summary (Enter / click expands, remembered for the session), `high` hunks get a full-cell gutter tint + a `◆ high` header badge and a dedicated `}`/`{` jump (plain `n`/`N` still walk all hunks). The diff always stays in file order — nothing is ever hidden or reordered, and a drifted range reverts to `normal` (under-collapse, never over-collapse). Nav header shows a static `N high · N routine` depth count. Report stored per authoring lane in `AcpHarnessView`, pulled on open + each auto-refresh through the compositor broker (`diff.review-priority` control op, no ViewBus broadcast), merged across lanes (max level per hunk). Silence degrades to today's full diff. Rust: one MCP tool (schema + round-trip in `hook_server.rs`). See `docs/160-diff-review-priority.md` / `docs/adr/0009`.
  - **Cross-file priority panel (`p`)** — a docked (right, 300px) table of contents over every reported range, `high` grouped first then `routine` (badge + `L<start>–<end>` + file path + optional reason). Variant B / **live preview**: `j`/`k` move the selection *and* the diff jumps + full-row-tints the region immediately (no `Enter`) — same-file scroll is instant, file switches debounced ~80ms to coalesce fast runs. `Esc` restores the pre-open position (cancelled browse); `Enter`/`q`/`p` keep the previewed one. Docks in a flex content row so the diff reflows to full width when hidden — never covers the diff. Re-maps on auto-refresh. Built only when the harness `reviewPriority` channel is wired.
- [x] ACP Harness lane monitor (live web dashboard) — a browser-opened dashboard that monitors harness lane activity in real time, built on the existing artifact + loopback-HTTP infrastructure (spec 133/149) plus one read-only telemetry channel. A `HarnessTelemetryPublisher` (`src/acp/harness-telemetry.ts`) subscribes to `LaneBus` + the triage/review stores and, on real source changes only (300 ms debounce, no periodic tick), folds them into a small harness-wide `TelemetrySnapshot` (lanes with exact `HarnessLaneStatus`, `observedTurns` audit proxy, attention/review/priority counts, foreign peers, a ≤14 closed-`EventKind` event ring) and `invoke`s `acp_publish_telemetry`. The Rust loopback server (`src-tauri/src/hook_server.rs`) caches it per-harness. The built-in server serves a **fixed standalone page** at `GET /dashboard` (`include_str!` of `src/acp/artifact-dashboard.html`) plus a token-free `GET /telemetry` (`{ harnesses: [...] }`); the page polls `/telemetry` ~1 s and renders an attention gauge + per-lane cards (status pill, attn/rev/pri/peer chips, activity-pulse canvas on active lanes) + a client-templated event stream. The server binds a **fixed default port 8765** (config-overridable, ephemeral fallback on conflict) so the dashboard is a stable, bookmarkable URL — `http://127.0.0.1:8765/dashboard` — opened in an external browser anytime via the app command (`Leader Shift+L` / command palette `Open Lane Monitor Dashboard` → `compositor.openDashboard()`) or the harness `#dashboard` command. Read-only; no token, no `tok/s`, no radar (cut for simplicity). Built `#polly`-style: design (spec 168) reviewed by Cursor-2 + Codex-4, then implemented as three cross-reviewed slices (Rust transport / TS publisher / dashboard) with 7 Rust + 6 TS tests. See `docs/168-harness-lane-monitor.md`.
- [x] ACP Harness dashboard resource status (spec 169) — surfaces per-lane CPU%/RSS on the lane monitor as a **live CPU sparkline + memory bar**, reusing the spec-91 metrics already sampled (`metricsBySession`) and the spec-168 telemetry pipeline. `TelemetryLane` gains `cpuPercent`/`rssMb`/`procCount`/`rootAlive`, folded in by a new `metricsFor(laneId)` accessor in `AcpHarnessView` (maps the publisher's string `laneId` → the lane's numeric ACP client session → metrics). Because a chart is only honest if its numbers move, the 2 s `pollMetrics()` also nudges the publisher to publish — **but only while ≥1 lane is active** (`anyLaneActive()` gate), so an idle harness still makes zero periodic pushes (the one deliberate deviation from spec 168's no-tick rule). The snapshot carries only the current sample; the time-series history is accumulated client-side in the dashboard, which repurposes the per-lane pulse canvas into a heat-coloured CPU sparkline (`rootAlive:false` → "—" + flat baseline). `TELEMETRY_SCHEMA_VERSION` bumped 1→2 in lockstep with the page's `SCHEMA_VERSION`; no Rust changes (value-opaque transport). Design reviewed by Codex-2 (architecture/correctness) + OpenCode-2 (requirements-fit) — drove the chips→chart pivot, the schema-constant lockstep catch, and the laneId→sessionId mapping. See `docs/169-dashboard-resource-status.md`.
- [x] ACP Harness docs browser (spec 171) — a read-only loopback web surface that renders the markdown files already present in a harness's working directory (`<cwd>`) in the OS browser: the browser-facing reader for a repo's `docs/`, ADRs, README, and code wiki, and a sibling of the lane monitor (spec 168) + artifact gallery (spec 170). `GET /docs` serves a fixed standalone page (`src/acp/artifact-docs.html`, file-tree sidebar + content pane, near-zero JS — folders are native `<details>`); `GET /doc?harness=&path=` renders one `.md` server-side via comrak (GFM, `unsafe_=false` + `escape=true` so raw HTML is escaped not executed, relative `.md` links rewritten to `/doc?…` and relative images to `/doc-asset?…`); `GET /doc-asset?harness=&path=` serves whitelisted image bytes. Files are discovered per-harness from `HarnessArtifactStore.project_dir` via `ignore::WalkBuilder` (respects `.gitignore`, skips `.git`), scanned on request (no cache, no tick). **Addressed by repo-relative path with no token** (ADR-0010) — the security boundary is `validate_doc_path` (canonicalize + component containment under `<cwd>` + symlink-escape rejection + extension gate), keeping per-file URLs bookmarkable, consistent with the token-free `/telemetry` + `/gallery` posture. comrak built `default-features = false` (drops syntect/onig/clap). Opened via `compositor.openDocs()` / command palette `Open Docs Browser` (`docs.open`) / the harness `#docs` command (mirrors gallery wiring; no keybinding). Built `#polly`-style: grilled design (spec 171 + ADR-0010), two cross-reviewed slices (Codex-1 backend / Cursor-1 page+wiring), 2 blockers + 5 warnings found and resolved. See `docs/171-docs-browser.md`.
- [x] Oscilloscope header band (spec 188) — the static tick strip below the titlebar (`.krypton-window__header-accent`) is replaced on terminal + Quick Terminal windows by a live **oscilloscope trace** on a per-window `<canvas>`, driven by real PTY output throughput. A new `HeaderScope` (`src/header-scope.ts`, implements the `BackgroundAnimation` interface) owns the canvas: a ring buffer strokes a scrolling glowing waveform whose amplitude tracks bytes/sec; the central `pty-output` router pumps `data.length` into the window's (or QT's) scope. The rAF loop **stops when energy + buffer decay to ~0** (≈0.6 s after output ceases) so an idle window's band costs 0 CPU — honoring the <1% idle budget; `prefers-reduced-motion` renders a single static hairline and never animates. Unfocused windows animate too (background-activity signal). DPR-aware via `ResizeObserver` (no compositor relayout coupling); accent read from `--krypton-window-accent-rgb`, refreshed on theme change; disposed on window/QT close. Reversible via new theme config `chrome.header_accent.style = "oscilloscope" | "ticks"` (default `oscilloscope`). Content-view (agent/vault) windows and dashboard panels keep the static band. See `docs/188-oscilloscope-header-band.md`.
- [ ] Edge cases: rapid workspace switching, many windows, large scrollback, resolution changes
- [ ] Bug fixes

## M9 — Release (Week 28-30)

- [ ] Platform packaging: DMG (macOS), AppImage/deb (Linux), MSI (Windows)
- [ ] Auto-update mechanism (Tauri updater)
- [ ] User documentation (keyboard cheat sheet, workspace config guide, custom theme guide, sound pack authoring guide) — theme specification draft created (`docs/10-theme-specification.md`)
- [ ] First public release
